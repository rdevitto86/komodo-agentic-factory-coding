diff --git a/internal/wallet/account.go b/internal/wallet/account.go
--- a/internal/wallet/account.go
+++ b/internal/wallet/account.go
@@ -10,8 +10,11 @@ type Account struct {
 	Balance int64
 }

-// Withdraw takes amount cents from the account.
+// Withdraw takes amount cents from the account, refusing to overdraw it.
 func (a *Account) Withdraw(amount int64) error {
+	if a.Balance > amount {
+		return ErrInsufficient
+	}
 	a.Balance -= amount
 	return nil
 }
