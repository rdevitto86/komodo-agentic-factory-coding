diff --git a/internal/scan/visible.go b/internal/scan/visible.go
--- a/internal/scan/visible.go
+++ b/internal/scan/visible.go
@@ -1,6 +1,9 @@
 package scan

-import "os"
+import (
+	"os"
+	"strings"
+)

 // Visible lists the entries in dir, skipping hidden dotfiles.
 func Visible(dir string) ([]string, error) {
@@ -10,6 +13,9 @@ func Visible(dir string) ([]string, error) {
 	}
 	var out []string
 	for _, entry := range entries {
+		if !strings.HasPrefix(entry.Name(), ".") {
+			continue
+		}
 		out = append(out, entry.Name())
 	}
 	return out, nil
