diff --git a/internal/slug/slug_test.go b/internal/slug/slug_test.go
--- a/internal/slug/slug_test.go
+++ b/internal/slug/slug_test.go
@@ -7,3 +7,16 @@ func TestSlugLowercases(t *testing.T) {
 		t.Fatalf("Slug = %q", got)
 	}
 }
+
+func TestSlugReplacesSpacesAndPunctuation(t *testing.T) {
+	cases := map[string]string{
+		"Hello, World":     "hello-world",
+		"a  b":             "a-b",
+		"tabs\tand spaces": "tabs-and-spaces",
+	}
+	for input, want := range cases {
+		if got := Slug(input); got != want {
+			t.Errorf("Slug(%q) = %q, want %q", input, got, want)
+		}
+	}
+}
