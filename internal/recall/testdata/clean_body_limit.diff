diff --git a/internal/fetch/body.go b/internal/fetch/body.go
--- a/internal/fetch/body.go
+++ b/internal/fetch/body.go
@@ -2,11 +2,15 @@ package fetch

 import (
 	"context"
+	"fmt"
 	"io"
 	"net/http"
 )

-// Body returns the response body at url.
+// maxBody caps a response body at one mebibyte.
+const maxBody = 1 << 20
+
+// Body returns the response body at url, failing on a non-200 status or a body over maxBody.
 func Body(ctx context.Context, client *http.Client, url string) ([]byte, error) {
 	req, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)
 	if err != nil {
@@ -17,5 +21,15 @@ func Body(ctx context.Context, client *http.Client, url string) ([]byte, error)
 		return nil, err
 	}
 	defer res.Body.Close()
-	return io.ReadAll(res.Body)
+	if res.StatusCode != http.StatusOK {
+		return nil, fmt.Errorf("%s answered %d", url, res.StatusCode)
+	}
+	data, err := io.ReadAll(io.LimitReader(res.Body, maxBody+1))
+	if err != nil {
+		return nil, err
+	}
+	if len(data) > maxBody {
+		return nil, fmt.Errorf("%s answered more than %d bytes", url, maxBody)
+	}
+	return data, nil
 }
