diff --git a/internal/tidy/prune.go b/internal/tidy/prune.go
--- a/internal/tidy/prune.go
+++ b/internal/tidy/prune.go
@@ -18,13 +18,15 @@ func runOpen(root string) bool {
 	return err == nil
 }

-// Prune removes the last run's worktree once it is clean and merged, never while a run is open.
-func Prune(root string, last Worktree) error {
-	if runOpen(root) {
-		return nil
+// Prune removes every worktree that is clean and merged, so no finished run strands a checkout.
+func Prune(root string, trees []Worktree) error {
+	for _, tree := range trees {
+		if !tree.Clean || !tree.Merged {
+			continue
+		}
+		if err := os.RemoveAll(tree.Path); err != nil {
+			return err
+		}
 	}
-	if last.Clean && last.Merged {
-		return os.RemoveAll(last.Path)
-	}
 	return nil
 }
