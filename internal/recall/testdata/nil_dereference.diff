diff --git a/internal/session/store.go b/internal/session/store.go
--- a/internal/session/store.go
+++ b/internal/session/store.go
@@ -24,3 +24,9 @@ func (s *Store) User(token string) (string, error) {
 	}
 	return session.User, nil
 }
+
+// IsAdmin reports whether the token's session holds admin rights.
+func (s *Store) IsAdmin(token string) bool {
+	session := s.byToken[token]
+	return session.Admin
+}
