# TODO

Open work only. A completed item's line is deleted in the same change that completes it — `git log TODO.md` is the record.

Severity: [C] Critical · [H] High · [M] Medium · [L] Low
Status: `[ ]` not started · `[~]` in progress

Groups are `Cross-Cutting` or a feature/subfolder name. Phases inside a group are small, sequential by default (`(parallel with Phase N)` marks an exception), and each phase that adds behavior carries its own unit/component test item — plus a contract item when it changes a published contract. Those three tiers gate the merge. Integration, smoke, live-dependency, and perf run after the merge and get their own later phase per group.

## Now — V1

### Cross-Cutting
#### Phase 1 · Project setup
- [ ] [H] Wire the health endpoint and the extended health check smoke depends on · S
- [ ] [M] Install the SDK git hooks and point `core.hooksPath` at them · S
- [ ] [H] PR pipeline: lint → unit → component → contract, cancel-superseded, path filters, caching · M
- [ ] [H] Release pipeline: build → deploy green → smoke → flip → integration → e2e · M

## Next — V2

_Nothing planned yet. Run `/plan` to fill this in._
