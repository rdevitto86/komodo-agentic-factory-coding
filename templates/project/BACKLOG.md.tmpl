# Project Backlog

Priority `[P: C|H|M|L]`. Status `[REFINEMENT|READY|IN_PROGRESS|BLOCKED|DONE]`. Ids `EPIC-XX` > `TG-XX.Y` > `TSK-XX.Y.Z`. The grammar the line parses is in the `backlog` skill; `komodo lint` checks it.

---

## [EPIC-01] First release
*Goal: the outcome this epic delivers, in one sentence.*

### [TG-01.1] Example group
```yaml
type: feat
mode: parallel
version: 0.1.0
```

#### [TSK-01.1.1] Replace this example with the first real task [P: H] [REFINEMENT]
```yaml
files: [path/to/file, path/to/file_test]
done_when:
  - test -f path/to/file
context: [docs/system-design.md#interfaces]
```

#### [TSK-01.1.2] A task that waits on the first one [P: M] [REFINEMENT]
```yaml
files: [path/to/other]
done_when:
  - test -f path/to/other
depends_on: [TSK-01.1.1]
```
