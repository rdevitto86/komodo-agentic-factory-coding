# Architecture — <Repo Name>

The stable shape of the system: small enough for a planner and a reviewer to read whole. Detail lives in `system-design.md`; product scope lives in `prd.md`. Every heading here exists in exactly one spec file, so a task cites one place.

## Purpose

<What the system does and for whom, in one paragraph.>

## Components

<One line per component: name, responsibility, where it runs.>

## Boundaries

<What is inside the system, what it depends on, and what it never does.>

## Data flow

<How a request or an event moves through the components, start to finish.>

## Decisions

<Dated entries appended in place, never rewritten: the decision, the alternative rejected, and why.>
