---
paths: ["internal/**"]
---

# Example repo context

Delete this file, or replace it with what a builder must know about this
repo's `internal/` code that the shipped rules and standards do not say.
Drop the `paths` list to have this file reach every task instead of only
the ones whose files match a glob here.
