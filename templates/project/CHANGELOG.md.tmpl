# Changelog

Notable changes to {{NAME}}. Format follows Keep a Changelog; versions follow SemVer.

Append-only — a released section is never rewritten. Format rules live in the `worklog` skill.

## [0.1.0] — {{DATE}}

### Added
- Initial repository skeleton.
