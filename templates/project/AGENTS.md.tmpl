# {{NAME}}

{{ONE_LINE_PURPOSE}}

Global rules live in `~/.claude/AGENTS.md` and always apply. This file adds project context only — see the `write-repo` skill for what belongs here and why.

## Quick reference

| Key | Value |
|---|---|
| Language | {{LANGUAGE}} |
| Port(s) | {{PORT}} |
| Contract | {{CONTRACT}} |
| Entrypoint | {{ENTRYPOINT}} |

## Commands

Every command an agent would otherwise have to guess or discover. Wrong here is worse than absent. Drop a row the repo genuinely has no equivalent for — never fill one with a guess.

| Do | Run |
|---|---|
| Verify (the Stop gate runs this) | `{{VERIFY}}` |
| Test | `{{TEST}}` |
| Build | `{{BUILD}}` |
| Run locally | `{{RUN}}` |
| Deploy | `{{DEPLOY}}` |
| Lint | `{{LINT}}` |

## Where things are

Only the paths that are not obvious from the language skill's standard layout. A repo matching it leaves this section out.

{{NON_OBVIOUS_PATHS}}

## Gotchas

Non-obvious behaviour that has already cost someone a debugging session. One line each, empirical only — never a restatement of a global rule.

{{GOTCHAS}}

## Documents

Only files that exist in this repo. A row is added the day its file is created — never listed in advance.

{{DOCUMENTS_TABLE}}

Design decisions live in the SDD's §11 (a Google Doc in Drive, fetched via MCP — see `standards-specs`), including any that earned a full write-up, appended there directly rather than as a separate file. Fetch on demand — never pre-load or cache it locally.

## Deviations

Where this repo diverges from a skill it would otherwise follow — language, `sdlc`, `cicd`, `security`, or any other — tagged with which skill it departs from. Refresh-only; a new repo has no drift yet.

{{DEVIATIONS}}
