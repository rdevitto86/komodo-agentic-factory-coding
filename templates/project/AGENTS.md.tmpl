# {{NAME}}

{{ONE_LINE_PURPOSE}}

Komodo's universal rules are rendered by `komodo install` and always apply. This file adds only what an agent would otherwise have to guess.

## Quick reference

| Key | Value |
|---|---|
| Language | {{LANGUAGE}} |
| Port(s) | {{PORT}} |
| Entrypoint | {{ENTRYPOINT}} |

## Commands

Wrong here is worse than absent. Drop a row the repo has no equivalent for.

| Do | Run |
|---|---|
| Verify (the harness and the pre-push hook run this) | `{{VERIFY}}` |
| Test | `{{TEST}}` |
| Build | `{{BUILD}}` |
| Run locally | `{{RUN}}` |
| Lint | `{{LINT}}` |

## Harness

`BACKLOG.md` is the queue, in the grammar the `backlog` skill describes. `komodo lint` checks it after every edit, and `komodo run` takes the next ready group. The line needs no config file.

## Where things are

Only paths that are not obvious from the language's standard layout.

{{NON_OBVIOUS_PATHS}}

## Gotchas

Non-obvious behaviour that has already cost someone a debugging session. One line each.

{{GOTCHAS}}

## Deviations

Where this repo departs from a standard, and which one.

{{DEVIATIONS}}
