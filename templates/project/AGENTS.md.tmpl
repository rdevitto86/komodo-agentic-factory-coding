# {{NAME}}

{{ONE_LINE_PURPOSE}}

Global rules live in `~/.claude/AGENTS.md` and always apply. This file adds only what an agent would otherwise have to guess.

## Quick reference

| Key | Value |
|---|---|
| Language | {{LANGUAGE}} |
| Port(s) | {{PORT}} |
| Entrypoint | {{ENTRYPOINT}} |

## Commands

Wrong here is worse than absent. Drop a row the repo has no equivalent for.

| Do | Run |
|---|---|
| Verify (the harness and the pre-push hook run this) | `{{VERIFY}}` |
| Test | `{{TEST}}` |
| Build | `{{BUILD}}` |
| Run locally | `{{RUN}}` |
| Lint | `{{LINT}}` |

## Harness

`BACKLOG.md` is the queue, in the grammar the `backlog` skill describes. `python3 -m komodo run` takes the next open group. The harness needs no config file: it reads its defaults from `komodo/config.py` and the account's own limits at preflight.

## Where things are

Only paths that are not obvious from the language's standard layout.

{{NON_OBVIOUS_PATHS}}

## Gotchas

Non-obvious behaviour that has already cost someone a debugging session. One line each.

{{GOTCHAS}}

## Deviations

Where this repo departs from a standard, and which one.

{{DEVIATIONS}}
