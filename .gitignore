# Local working files — never committed. See CLAUDE.md "Working files".
# Anchored to root so the lowercase convention specs under
# claude/agents/project-manager/ (todo.md, memory.md) stay tracked even with
# core.ignorecase=true.
/TODO.md
/MEMORY.md

# Editor / OS noise
.DS_Store
