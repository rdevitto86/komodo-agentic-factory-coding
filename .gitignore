# Local session and run state, never shared
.claude/
.codex/
.agents/
/.komodo/

# Personal overlays the install seeds once and never overwrites
CLAUDE.local.md

# Editor / OS noise
.DS_Store

# Local build output, built by `komodo gate --install` and `komodo release build`
/bin/
/dist/
