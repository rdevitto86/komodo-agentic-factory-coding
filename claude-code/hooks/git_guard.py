#!/usr/bin/env python3

import json
import os
import re
import shlex
import subprocess
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from comment_guard import EXTENSION_FAMILY, FILENAME_FAMILY

READ_ONLY_GIT = {
    "annotate",
    "blame",
    "cat-file",
    "check-ignore",
    "cherry",
    "config",
    "count-objects",
    "describe",
    "diff",
    "diff-index",
    "diff-tree",
    "difftool",
    "fetch",
    "for-each-ref",
    "grep",
    "help",
    "log",
    "ls-files",
    "ls-remote",
    "ls-tree",
    "merge-base",
    "name-rev",
    "remote",
    "rev-list",
    "rev-parse",
    "shortlog",
    "show",
    "show-branch",
    "show-ref",
    "status",
    "tag",
    "var",
    "verify-commit",
    "version",
    "whatchanged",
}

MUTATING_FLAGS = {
    "tag": ("-d", "-D", "--delete", "-f", "--force"),
    "remote": ("add", "remove", "rm", "rename", "set-url", "set-head", "set-branches", "prune"),
    "config": ("--unset", "--unset-all", "--add", "--replace-all", "--rename-section", "--remove-section", "--edit", "-e"),
}

GIT_GLOBAL_FLAGS_WITH_VALUE = ("-C", "-c", "--git-dir", "--work-tree", "--namespace", "--exec-path")

# --- Publishing policy -------------------------------------------------
#
# The agent branches, commits, pushes its own branch, opens a PR, and
# may sync its branch with its protected base to resolve conflicts
# before a human merges — it never reaches a protected ref, never
# rewrites history, and never lands into one. `rules-source-control`
# carries the reasoning, this is only the deterministic half of the rule.
#
# Externalized rather than a bare constant: flip with
# `PUBLISH_ENABLED=0`, no source edit needed to retire the capability.
PUBLISH_ENABLED = os.environ.get("PUBLISH_ENABLED", "1") != "0"

PROTECTED_BRANCHES = {"main", "master", "trunk", "prod", "production"}
PROTECTED_PREFIXES = ("release/", "hotfix/")

BRANCH_NAME = re.compile(r"^(feat|fix|chore|docs|test|refactor|perf|build|ci)/[a-z0-9][a-z0-9-]*$")

BRANCH_MUTATING_FLAGS = ("-d", "-D", "-m", "-M", "-c", "-C", "--delete", "--move", "--copy", "--edit-description", "--set-upstream-to", "-u", "--unset-upstream")
SWITCH_BANNED_FLAGS = ("-C", "--force-create", "--orphan", "-d", "--detach", "--discard-changes")
ADD_BANNED_FLAGS = ("-i", "--interactive", "-p", "--patch")
COMMIT_BANNED_FLAGS = ("-n", "--no-verify", "--amend", "--no-gpg-sign")
PUSH_FORCE_FLAGS = ("-f", "--force", "--force-with-lease", "--force-if-includes")
PUSH_BANNED_FLAGS = ("--mirror", "--all", "--tags", "--follow-tags", "--delete", "-d", "--prune")
STASH_MUTATING_FLAGS = ("drop", "clear")

# WHY: only strategy options that auto-resolve without a visible conflict
# are banned — a plain merge that stops and leaves conflict markers is
# exactly the case this capability exists for.
MERGE_BANNED_FLAGS = ("-X", "--strategy-option", "-s", "--strategy", "--squash")
MERGE_SAFE_FLAGS = ("--abort", "--continue")

BANNED_TRAILER = re.compile(r"co-authored-by\s*:|generated with|generated by|\U0001F916", re.I)

# A -m body can span newlines, and SEGMENT_SPLIT below breaks on them, so a
# trailer on the message's third line never reaches the tokenized args —
# the raw, unsplit command text is the only place the whole message
# is still intact. Covers `commit` and `merge -m` alike — both can carry
# a message with a trailer.
GIT_MESSAGE_CALL = re.compile(r"\bgit\b[^\n;|&]*\b(?:commit|merge)\b")

is_protected = lambda name: name in PROTECTED_BRANCHES or name.startswith(PROTECTED_PREFIXES)

GH_API_WRITE_FLAGS = ("-X", "--method", "-f", "--raw-field", "-F", "--field", "--input")
GH_ALLOWED = {
    "pr": {"create", "view", "list", "diff", "status", "checks", "comment", "edit", "ready"},
    "issue": {"view", "list", "create", "comment"},
    "repo": {"view"},
    "run": {"list", "view", "watch"},
    "label": {"list"},
    "auth": {"status"},
    "search": None,
    "browse": None,
    "status": None,
    "version": None,
    "extension": {"list"},
}
# `pr edit`/`pr comment` take a PR number as their first positional and
# reach outside the agent's own branch if left unscoped — verified
# against the branch's own open PR before either is allowed through.
GH_PR_SCOPED_ACTIONS = {"edit", "comment"}

SHELL_WRAPPERS = ("sh", "bash", "zsh", "dash", "ksh", "env")
PASSTHROUGH_WRAPPERS = ("time", "command", "nohup", "xargs")
PASSTHROUGH_VALUE_FLAGS = {
    "time": ("-f", "-o"),
    "command": (),
    "nohup": (),
    "xargs": ("-I", "-n", "-P", "-L", "-s", "-a", "-d", "-E"),
}

# Names strip_leading_flags treats as "this must be the wrapped command,
# not a flag's value" when it meets an option it doesn't recognize.
MONITORED_COMMANDS = SHELL_WRAPPERS + PASSTHROUGH_WRAPPERS + ("git", "gh", "cp", "mv", "tee", "eval")

CP_MV_TARGET_FLAGS = ("-t", "--target-directory")

SEGMENT_SPLIT = re.compile(r"&&|\|\||[;\n|]")
REDIRECT = re.compile(r"(?<![-=<0-9&])>>?\s*([^\s;&|>]+)")
HEREDOC = re.compile(r"(<<-?\s*['\"]?(\w+)['\"]?[^\n]*\n)(.*?)(^\s*\2\s*$)", re.S | re.M)
QUOTED = re.compile(r"'[^']*'|\"[^\"]*\"")
INPLACE_SED = re.compile(r"\bsed\b[^;|&]*?(?:\s-[a-zA-Z]*i\b|\s--in-place\b)")
INPLACE_PERL = re.compile(r"\bperl\b[^;|&]*?\s-[a-zA-Z]*i\b")
PYTHON_WRITE = re.compile(r"\bpython3?\b[^;|&]*?-c\b.*?open\s*\([^)]*['\"][wa]")


def parse_cp_mv_target(tokens):
    # cp/mv -t DIR (or --target-directory[=DIR]) names the real
    # destination out of order; everything else stays positional.
    index = 0
    target_dir = None
    positional = []
    while index < len(tokens):
        token = tokens[index]
        if token in CP_MV_TARGET_FLAGS:
            if index + 1 < len(tokens):
                target_dir = tokens[index + 1]
            index += 2
            continue
        if token.startswith("--target-directory="):
            target_dir = token.split("=", 1)[1]
            index += 1
            continue
        if token.startswith("-") and token != "-":
            index += 1
            continue
        positional.append(token)
        index += 1
    return target_dir, positional


def is_code_path(token):
    cleaned = token.strip("\"'")
    base = os.path.basename(cleaned)
    if base in FILENAME_FAMILY:
        return True
    _, ext = os.path.splitext(base)
    return ext.lower() in EXTENSION_FAMILY


def blank(text):
    return re.sub(r"[^\n]", " ", text)


def mask_data(command):
    # heredoc bodies and quoted spans are data, not shell syntax
    masked = HEREDOC.sub(lambda m: m.group(1) + blank(m.group(3)) + m.group(4), command)
    return QUOTED.sub(lambda m: blank(m.group(0)), masked)


def tokenize(segment):
    try:
        return shlex.split(segment)
    except ValueError:
        return segment.split()


def strip_env_assignments(tokens):
    index = 0
    while index < len(tokens) and re.match(r"^[A-Za-z_][A-Za-z0-9_]*=", tokens[index]):
        index += 1
    return tokens[index:]


def strip_leading_flags(tokens, value_flags):
    index = 0
    while index < len(tokens) and tokens[index].startswith("-"):
        flag = tokens[index]
        index += 1
        if flag in value_flags:
            index += 1
            continue
        if "=" in flag:
            continue
        # An option we don't have on record: if what follows doesn't look
        # like the wrapped command itself, assume it's this flag's value
        # rather than mis-starting the inner command at that token.
        if index < len(tokens):
            nxt = tokens[index]
            if not nxt.startswith("-") and os.path.basename(nxt) not in MONITORED_COMMANDS:
                index += 1
    return tokens[index:]


def git_subcommand(tokens):
    index = 1
    while index < len(tokens):
        token = tokens[index]
        if token in GIT_GLOBAL_FLAGS_WITH_VALUE:
            index += 2
            continue
        if token.startswith("-"):
            index += 1
            continue
        return token, tokens[index + 1:]
    return None, []


def current_branch():
    try:
        result = subprocess.run(
            ["git", "rev-parse", "--abbrev-ref", "HEAD"],
            capture_output=True, text=True, timeout=5,
        )
    except (OSError, subprocess.SubprocessError):
        return None
    if result.returncode != 0:
        return None
    return result.stdout.strip() or None


def publish_violation(subcommand, args):
    positional = [arg for arg in args if not arg.startswith("-")]

    if subcommand == "switch":
        for flag in SWITCH_BANNED_FLAGS:
            if flag in args:
                return "git switch %s discards or detaches state" % flag
        if "-c" in args or "-C" in args:
            target = positional[0] if positional else None
            # WHY: no separate is_protected check here — BRANCH_NAME's type
            # list shares no entry with PROTECTED_BRANCHES/PREFIXES, so a
            # protected name always fails this match first.
            if not target or not BRANCH_NAME.match(target):
                return "git switch -c needs a <type>/<short-kebab-description> branch name"
        return None

    if subcommand == "branch":
        for flag in BRANCH_MUTATING_FLAGS:
            if flag in args:
                return "git branch %s changes repository state" % flag
        if positional:
            target = positional[0]
            if not BRANCH_NAME.match(target):
                return "git branch %s needs a <type>/<short-kebab-description> name" % target
        return None

    if subcommand == "add":
        for flag in ADD_BANNED_FLAGS:
            if flag in args:
                return "git add %s needs interactive input" % flag
        return None

    if subcommand == "commit":
        for flag in COMMIT_BANNED_FLAGS:
            if flag in args:
                return "git commit %s bypasses review or rewrites history" % flag
        branch = current_branch()
        if branch and is_protected(branch):
            return "committing directly on %s, a protected branch" % branch
        return None

    if subcommand == "push":
        for flag in PUSH_FORCE_FLAGS:
            if flag in args:
                return "git push %s rewrites remote history" % flag
        for flag in PUSH_BANNED_FLAGS:
            if flag in args:
                return "git push %s reaches beyond the current branch" % flag
        target = positional[1] if len(positional) >= 2 else None
        branch = target or current_branch()
        if branch and is_protected(branch.split(":")[0]):
            return "git push targets %s, a protected branch" % branch
        return None

    if subcommand == "stash":
        if positional and positional[0] in STASH_MUTATING_FLAGS:
            return "git stash %s discards saved state" % positional[0]
        return None

    if subcommand == "merge":
        if any(flag in args for flag in MERGE_SAFE_FLAGS):
            return None
        for flag in MERGE_BANNED_FLAGS:
            if flag in args:
                return "git merge %s auto-resolves without a visible conflict" % flag
        branch = current_branch()
        if branch and is_protected(branch):
            return "git merge on %s, a protected branch — landing into it stays the user's job" % branch
        if len(positional) != 1:
            return "git merge only takes one target: the protected base branch, to sync before a human merges the PR"
        target = positional[0]
        bare = target[len("origin/"):] if target.startswith("origin/") else target
        if not is_protected(bare):
            return "git merge %s isn't the protected base branch" % target
        return None

    return None


def git_violation(subcommand, args):
    if subcommand is None:
        return None

    if subcommand == "switch":
        if not PUBLISH_ENABLED:
            return "git switch changes repository state"
        return publish_violation(subcommand, args)

    if subcommand == "branch":
        positional = [arg for arg in args if not arg.startswith("-")]
        mutating = bool(positional) or any(flag in args for flag in BRANCH_MUTATING_FLAGS)
        if not mutating:
            return None  # plain listing: `git branch`, `git branch -a`, `--show-current`
        if not PUBLISH_ENABLED:
            return "git branch changes repository state"
        return publish_violation(subcommand, args)

    if subcommand == "stash":
        positional = [arg for arg in args if not arg.startswith("-")]
        if positional and positional[0] in ("list", "show"):
            return None  # explicit read-only mode; bare `git stash` defaults to push/save
        if not PUBLISH_ENABLED:
            return "git stash changes repository state"
        return publish_violation(subcommand, args)

    if subcommand in ("commit", "push", "add", "merge"):
        if not PUBLISH_ENABLED:
            return "git %s changes repository state" % subcommand
        return publish_violation(subcommand, args)

    if subcommand not in READ_ONLY_GIT:
        return "git %s changes repository state" % subcommand
    if subcommand == "config":
        positional = [arg for arg in args if not arg.startswith("-")]
        if len(positional) >= 2:
            return "git config %s sets a value" % positional[0]
    mutators = MUTATING_FLAGS.get(subcommand)
    if not mutators:
        return None
    for arg in args:
        if arg in mutators:
            return "git %s %s changes repository state" % (subcommand, arg)
    return None


def gh_subcommand_path(tokens):
    index = 0
    path = []
    while index < len(tokens):
        token = tokens[index]
        if token in ("-R", "--repo"):
            index += 2
            continue
        if token.startswith("-"):
            index += 1
            continue
        path.append(token)
        index += 1
        if len(path) == 2 or path[0] not in GH_ALLOWED or GH_ALLOWED.get(path[0]) is None:
            break
    return path, tokens[index:]


def current_branch_pr_number():
    try:
        result = subprocess.run(
            ["gh", "pr", "view", "--json", "number", "-q", ".number"],
            capture_output=True, text=True, timeout=10,
        )
    except (OSError, subprocess.SubprocessError):
        return None
    if result.returncode != 0:
        return None
    value = result.stdout.strip()
    return value or None


def gh_violation(tokens):
    if tokens and tokens[0] == "api":
        for flag in GH_API_WRITE_FLAGS:
            if flag in tokens[1:]:
                return "gh api %s writes through the API, bypassing every other check" % flag
        return None

    path, rest = gh_subcommand_path(tokens)
    if not path:
        return "gh with no subcommand is not on the allowlist"
    top = path[0]
    if top not in GH_ALLOWED:
        return "gh %s is not on the allowlist" % top
    actions = GH_ALLOWED[top]
    if actions is None:
        return None
    if len(path) < 2 or path[1] not in actions:
        return "gh %s %s is not on the allowlist" % (top, path[1] if len(path) > 1 else "")

    action = path[1]
    if top == "pr" and action in GH_PR_SCOPED_ACTIONS:
        target = next((tok for tok in rest if not tok.startswith("-")), None)
        own_pr = current_branch_pr_number()
        if own_pr is None:
            return "gh pr %s couldn't confirm this branch's own PR number" % action
        if target and target != own_pr:
            return "gh pr %s %s reaches a PR that isn't this branch's own (#%s)" % (action, target, own_pr)
    return None


def scan_segment(segment, findings):
    tokens = strip_env_assignments(tokenize(segment))
    if not tokens:
        return
    command = os.path.basename(tokens[0])
    if command in SHELL_WRAPPERS:
        for index, token in enumerate(tokens):
            if token == "-c" and index + 1 < len(tokens):
                scan_command(tokens[index + 1], findings)
        return
    if command == "eval":
        inner = tokens[1:]
        if inner:
            scan_command(" ".join(inner), findings)
        return
    if command in PASSTHROUGH_WRAPPERS:
        inner = strip_leading_flags(tokens[1:], PASSTHROUGH_VALUE_FLAGS[command])
        if inner:
            scan_command(" ".join(inner), findings)
        return
    if command == "tee":
        for token in tokens[1:]:
            if is_code_path(token):
                findings.append("tee writing to %s bypasses the comment guard" % token)
                break
        return
    if command in ("cp", "mv"):
        target_dir, positional = parse_cp_mv_target(tokens[1:])
        if target_dir is not None:
            if is_code_path(target_dir):
                findings.append("%s writing to %s bypasses the comment guard" % (command, target_dir))
                return
            for source in positional:
                if is_code_path(source):
                    findings.append("%s writing to %s bypasses the comment guard" % (command, target_dir))
                    break
            return
        if not positional:
            return
        destination = positional[-1]
        sources = positional[:-1]
        dest_clean = destination.strip("\"'")
        stripped = dest_clean.rstrip("/")
        _, dest_ext = os.path.splitext(stripped)
        dest_is_dir = (
            dest_clean.endswith("/")
            or (bool(stripped) and os.path.isdir(stripped))
            or (bool(stripped) and not dest_ext and not os.path.isfile(stripped))
        )
        if dest_is_dir:
            for source in sources:
                if is_code_path(source):
                    findings.append("%s writing to %s bypasses the comment guard" % (command, destination))
                    break
        elif is_code_path(destination):
            findings.append("%s writing to %s bypasses the comment guard" % (command, destination))
        return
    if command == "git":
        subcommand, args = git_subcommand(tokens)
        violation = git_violation(subcommand, args)
        if violation:
            findings.append(violation)
        return
    if command == "gh":
        violation = gh_violation(tokens[1:])
        if violation:
            findings.append(violation)
        return


def scan_command(command, findings):
    for segment in SEGMENT_SPLIT.split(command):
        segment = segment.strip()
        if segment:
            scan_segment(segment, findings)

    if INPLACE_SED.search(command):
        findings.append("sed -i rewrites files in place, bypassing the comment guard")
    if INPLACE_PERL.search(command):
        findings.append("perl -i rewrites files in place, bypassing the comment guard")
    if PYTHON_WRITE.search(command):
        findings.append("python -c opening a file for writing bypasses the comment guard")
    if GIT_MESSAGE_CALL.search(command) and BANNED_TRAILER.search(command):
        findings.append("the commit message carries a co-author or generated-by trailer")

    for target in REDIRECT.findall(mask_data(command)):
        if is_code_path(target):
            findings.append("redirecting output into %s bypasses the comment guard" % target)


def respond_deny(reason):
    payload = {
        "hookSpecificOutput": {
            "hookEventName": "PreToolUse",
            "permissionDecision": "deny",
            "permissionDecisionReason": reason,
        }
    }
    sys.stdout.write(json.dumps(payload))
    sys.exit(0)


def main():
    payload = json.loads(sys.stdin.read())
    if payload.get("tool_name") != "Bash":
        sys.exit(0)
    command = (payload.get("tool_input") or {}).get("command", "")
    if not command:
        sys.exit(0)

    findings = []
    scan_command(command, findings)
    if not findings:
        sys.exit(0)

    unique = []
    for item in findings:
        if item not in unique:
            unique.append(item)

    lines = ["BLOCKED. Nothing ran.", ""]
    lines.extend("    %s" % item for item in unique)
    lines.extend([
        "",
        "The agent branches, commits, pushes its own branch, opens a PR, and",
        "may merge its protected base into its own branch to resolve conflicts.",
        "main/master/trunk/prod/production/release/*/hotfix/* stay off limits,",
        "and so do landing a branch into one, rebases, pulls, force-push,",
        "--amend, and history rewrites. Run this yourself if you intended it.",
    ])
    respond_deny("\n".join(lines))


if __name__ == "__main__":
    try:
        main()
    except SystemExit:
        raise
    except BaseException as error:
        respond_deny(
            "git guard failed to evaluate this command: %s: %s\nFailing closed."
            % (type(error).__name__, error)
        )
