---
name: standards-vue
description: Vue 3. Loads on matching paths; the rules live in one file.
paths: "**/*.vue, **/stores/**"
---

# Vue 3 standard

Read `~/.claude/standards/vue.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
