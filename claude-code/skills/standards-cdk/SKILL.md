---
name: standards-cdk
description: CDK and AWS. Loads on matching paths; the rules live in one file.
paths: "**/cdk/**, **/infra/**, **/infrastructure/**, **/*-stack.ts, **/*.stack.ts, **/cdk.json"
---

# CDK standard

Read `~/.claude/standards/cdk.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
