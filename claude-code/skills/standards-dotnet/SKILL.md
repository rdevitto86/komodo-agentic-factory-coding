---
name: standards-dotnet
description: .NET framework and runtime conventions. Loads on matching paths; the rules live in one file.
paths: "**/*.csproj, **/*.sln, **/Directory.Build.props, **/appsettings*.json, **/global.json, **/Program.cs"
---

# .NET standard

Read `~/.claude/standards/dotnet.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
