---
name: standards-aws
description: AWS conventions. Loads on matching paths; the rules live in one file.
paths: "**/*.tf, **/cdk/**, **/infra/**, **/infrastructure/**"
---

# AWS standard

Read `~/.claude/standards/aws.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
