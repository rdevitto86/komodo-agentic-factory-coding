---
name: standards-ui-mobile
description: Native mobile UI standards. Loads on matching paths; the rules live in one file.
paths: "**/*.swift, **/*.kt, **/*.m, **/*.mm, **/*.storyboard, **/*.xib, **/AndroidManifest.xml, **/Info.plist"
---

# Mobile UI standard

Read `~/.claude/standards/ui-mobile.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
