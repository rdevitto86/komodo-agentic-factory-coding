---
name: standards-hardware
description: Hardware and robotics. Loads on matching paths; the rules live in one file.
paths: "**/*.kicad_sch, **/*.kicad_pcb, **/*.sch, **/*.brd, **/bom*.csv"
---

# Hardware standard

Read `~/.claude/standards/hardware.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
