---
name: standards-c
description: C standards. Loads on matching paths; the rules live in one file.
paths: "**/*.c, **/*.h"
---

# C standard

Read `~/.claude/standards/c.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
