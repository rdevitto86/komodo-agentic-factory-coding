---
name: standards-go
description: Go standards. Loads on matching paths; the rules live in one file.
paths: "**/*.go, **/go.mod, **/go.sum"
---

# Go standard

Read `~/.claude/standards/go.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
