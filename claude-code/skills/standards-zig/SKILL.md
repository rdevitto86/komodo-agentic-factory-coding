---
name: standards-zig
description: Zig conventions. Loads on matching paths; the rules live in one file.
paths: "**/*.zig, **/build.zig, **/build.zig.zon"
---

# Zig standard

Read `~/.claude/standards/zig.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
