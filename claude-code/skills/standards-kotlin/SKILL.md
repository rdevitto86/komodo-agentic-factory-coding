---
name: standards-kotlin
description: Kotlin conventions. Loads on matching paths; the rules live in one file.
paths: "**/*.kt, **/*.kts"
---

# Kotlin standard

Read `~/.claude/standards/kotlin.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
