---
name: standards-svelte
description: Svelte 5 and SvelteKit. Loads on matching paths; the rules live in one file.
paths: "**/*.svelte, **/+page.ts, **/+page.server.ts, **/+layout.ts, **/+server.ts"
---

# Svelte 5 standard

Read `~/.claude/standards/svelte.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
