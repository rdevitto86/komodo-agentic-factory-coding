---
name: standards-shell
description: Shell/bash standards. Loads on matching paths; the rules live in one file.
paths: "**/*.sh, **/*.bash"
---

# Shell standard

Read `~/.claude/standards/shell.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
