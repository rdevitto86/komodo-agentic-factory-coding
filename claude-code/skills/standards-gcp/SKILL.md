---
name: standards-gcp
description: GCP conventions. Loads on matching paths; the rules live in one file.
paths: "**/*.gcp.*, **/cloudbuild*.yaml, **/app.yaml"
---

# GCP standard

Read `~/.claude/standards/gcp.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
