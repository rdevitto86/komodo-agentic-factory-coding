---
name: standards-api-design
description: API shape and contract conventions. Loads on matching paths; the rules live in one file.
paths: "**/api/**, **/routes/**, **/controllers/**, **/handlers/**, **/*.proto, **/openapi*, **/graphql/**"
---

# API design standard

Read `~/.claude/standards/api-design.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
