---
name: standards-rust
description: Rust standards. Loads on matching paths; the rules live in one file.
paths: "**/*.rs, **/Cargo.toml"
---

# Rust standard

Read `~/.claude/standards/rust.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
