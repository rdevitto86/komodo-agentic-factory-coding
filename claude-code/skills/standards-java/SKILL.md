---
name: standards-java
description: Java standards. Loads on matching paths; the rules live in one file.
paths: "**/*.java, **/pom.xml, **/build.gradle, **/build.gradle.kts"
---

# Java standard

Read `~/.claude/standards/java.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
