---
name: standards-react
description: React. Loads on matching paths; the rules live in one file.
paths: "**/*.tsx, **/*.jsx"
---

# React standard

Read `~/.claude/standards/react.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
