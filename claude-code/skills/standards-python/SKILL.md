---
name: standards-python
description: Python standards. Loads on matching paths; the rules live in one file.
paths: "**/*.py, **/*.pyi, **/pyproject.toml"
---

# Python standard

Read `~/.claude/standards/python.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
