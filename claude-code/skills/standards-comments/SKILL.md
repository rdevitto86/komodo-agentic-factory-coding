---
name: standards-comments
description: Comment rules for any source file. Loads on matching paths; the rules live in one file.
paths: "**/*.go, **/*.py, **/*.ts, **/*.tsx, **/*.js, **/*.jsx, **/*.rs, **/*.java, **/*.kt, **/*.swift, **/*.cs, **/*.c, **/*.h, **/*.cpp, **/*.zig, **/*.sh, **/*.sql, **/*.vue, **/*.svelte"
---

# Comments standard

Read `~/.claude/standards/comments.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
