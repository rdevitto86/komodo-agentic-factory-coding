---
name: standards-ui-web
description: Browser UI standards. Loads on matching paths; the rules live in one file.
paths: "**/*.css, **/*.svelte, **/*.vue, **/*.tsx, **/*.jsx, **/*.html"
---

# Web UI standard

Read `~/.claude/standards/ui-web.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
