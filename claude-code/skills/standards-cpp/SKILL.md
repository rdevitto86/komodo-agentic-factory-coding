---
name: standards-cpp
description: Embedded C/C++. Loads on matching paths; the rules live in one file.
paths: "**/*.cc, **/*.cpp, **/*.hpp, **/*.ino"
---

# Embedded C/C++ standard

Read `~/.claude/standards/cpp.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
