---
name: standards-typescript
description: TypeScript and JavaScript standards. Loads on matching paths; the rules live in one file.
paths: "**/*.ts, **/*.tsx, **/*.js, **/*.jsx, **/*.mjs, **/package.json, **/tsconfig.json"
---

# TypeScript and JavaScript standard

Read `~/.claude/standards/typescript.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
