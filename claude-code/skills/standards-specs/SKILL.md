---
name: standards-specs
description: Section maps and read contract for the SDD and the optional PRD. Loads on matching paths; the rules live in one file.
paths: "**/docs/spec/**"
---

# Specs: SDD and PRD standard

Read `~/.claude/standards/specs.md` before writing or reviewing a matching file, and follow it. It is the same file the harness injects into its workers, so a session and a worker hold the same rules.
