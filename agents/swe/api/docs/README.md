# api docs (mode-gated)

Targeted API/platform context loaded only when swe's `api` mode is active. Add markdown here (e.g. gateway conventions, shared error catalog, auth flows). Project-specific API context lives in the target repo's `/docs/`.
