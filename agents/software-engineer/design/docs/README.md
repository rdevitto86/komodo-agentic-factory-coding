# architecture docs (mode-gated)

Org-wide architecture context loaded only when software-engineer's `design` mode is active. Add system maps, ADRs, and integration diagrams here. Project-specific architecture lives in the target repo's `/docs/`.
