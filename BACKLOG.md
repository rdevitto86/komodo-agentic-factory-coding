# Project Backlog

## Convention Legend
* **Priority Tagging:** `[C]` Critical | `[H]` High | `[M]` Medium | `[L]` Low
* **Status Indicators:** `[TODO]` | `[IN_PROGRESS]` | `[BLOCKED]` | `[DONE]`
* **Hierarchy ID:** `EPIC-XX` -> `TG-XX.Y` (Task Group) -> `TSK-XX.Y.Z` (Task) -> `SUB-XX.Y.Z.N` (Subtask)

Format and rules live in the `backlog-modify` skill — load it before editing this file. `[DONE]` tasks stay until a sweep (`/backlog-audit`) moves them to `CHANGELOG.md` and removes them — this is not a log to hand-curate.

---

## [EPIC-01] Now, V1
*Goal: keep this toolkit's own hooks, docs, and skills correct and internally consistent.*

### [TG-01.1] Cross-Cutting
* **Target Release:** V1

#### [TSK-01.1.2] Bridge: `num_ctx` truncation on large summarizer payloads [P: M] [BLOCKED]
* **Blocked By:** `external`
  * **Reason (2026-08-28):** No file to edit in this repo — the bridge server (`generateRequest`, `agents.go`) lives in the separate `~/.komodo/bridge` deploy; `bridges/komodo-bridge/` here holds only prompt files and docs.
  * **Citation:** `bridges/komodo-bridge/` (prompt files and docs only, no Go source)
  * **Recheck:** bridge source is vendored into or made reachable from this repo — `find bridges/komodo-bridge -iname '*.go'` returns a match
* **SUB-01.1.2.1** fix `generateRequest`'s payload truncation against `num_ctx` in the bridge server once its source is reachable from this repo
  * **Done when:** a large summarizer payload no longer silently truncates against `num_ctx` in `~/.komodo/bridge`

#### [TSK-01.1.12] Open policy question: should "reflow a pre-existing wrapped line to the current convention whenever it's revisited during unrelated work" be a standing, written exception to this toolkit's own no-scope-expansion rule in `AGENTS.md`? [P: L] [TODO]
* **SUB-01.1.12.1** decide whether formatting-only drive-by fixes get a blanket exception (and if so, where that exception is written down — `standards-go`, `AGENTS.md`, or both) versus staying subject to the existing scope rule; a decision for the user, not something this review resolves on its own

#### [TSK-01.1.13] `lib/git.py`'s `repo_root()` only catches `(OSError, subprocess.SubprocessError)` around `subprocess.run(cwd=start, ...)`, so a non-str/bytes/PathLike `cwd` (e.g. an int from a malformed payload) raises an uncaught `TypeError` — surfaced by `scripts/test-hooks.sh`'s new R12 case, which only passes because `reviewer_guard.py`'s outer `except BaseException` in `__main__` rescues it; `git_guard.py` calls the same `repo_root` and fails closed by design, so the same uncaught `TypeError` there depends entirely on how far up its own exception handling reaches [P: L] [TODO]
* **SUB-01.1.13.1** widen `repo_root()`'s except clause (or validate `start` before the call) so the failure mode is deliberate rather than incidental to whichever caller's outer handler happens to catch it first
  * **Done when:** `repo_root()` handles a non-path `start` without relying on caller-level rescue · S → `/assess-bugs claude-code/hooks/lib/git.py` reports it clear

#### [TSK-01.1.14] `standards-go/SKILL.md`'s two new "modernize" transform bullets hardcode `Go 1.27+`/`Go 1.26+` version numbers [P: L] [TODO]
* **SUB-01.1.14.1** the same file's own "Version floor is whatever `go.mod` declares. Read it; never assume a release." convention (line 18), and root `AGENTS.md`'s "No static references" table, whose worked example is literally "Instead of `Go 1.26` write the floor `go.mod` declares" — but the two new bullets (`embedlit` composite-literal flattening, `errors.AsType[T]`) name the literal versions `Go 1.27+`/`Go 1.26+` directly rather than phrasing the gate relative to the floor
  * **Done when:** the two bullets phrase their version gate without a hardcoded release number (e.g. "once `go.mod`'s floor reaches the release that added it"), consistent with line 18 and `AGENTS.md`'s own example · S → `/assess-bugs claude-code/skills/standards-go/SKILL.md` reports it clear

#### [TSK-01.1.15] `segment_wants_reparse`'s `command`-prefix handling treats `command -v`/`-V` (existence/type checks that never execute their argument) the same as `command eval` (which does execute it), producing a verified false-positive deny [P: M] [TODO]
* **SUB-01.1.15.1** `PASSTHROUGH_VALUE_FLAGS["command"]` is `()`, so `strip_leading_flags` walks past `-v`/`-V` as unrecognized flags without excluding them; `segment_wants_reparse('command -v eval ')` returns `True` even though real bash's `command -v eval` only reports whether `eval` exists and never re-parses its arguments — confirmed end-to-end: `analyze({'tool_name': 'Bash', 'tool_input': {'command': 'command -v eval "$(echo \\`git push --force\\`)"'}})` returns `['git push --force rewrites published history']`, denying a command bash would execute as a no-op existence check. `CHANGELOG.md`'s `[Unreleased]` entry ("looks past a `command` prefix ... at what it actually runs") doesn't mention this gap and overstates precision for check-only flags
  * **Done when:** `segment_wants_reparse` does not classify a `command -v`/`command -V` invocation as reparse-True, and the `git_guard.py` false positive above is allowed · S → `/assess-security claude-code/hooks/git_guard.py` reports it clear

#### [TSK-01.1.16] `extract_substitutions` grew its own second copy of `split_segments`'s quote/comment/boundary state machine instead of reusing it [P: M] [TODO]
* **SUB-01.1.16.1** `extract_substitutions` added `segment_start` tracking (squote/dquote/comment state, `&&`/`||`/`SEGMENT_BOUNDARY_CHARS` resets) purely so `segment_wants_reparse` can recover "what command led the enclosing segment" — but `split_segments` already walks the exact same command string tracking the exact same quote/comment/boundary state to produce that same segment slicing, as a separate pass immediately after. The two hand-rolled lexers must now be kept in lockstep by hand (a comment-boundary reset was once added to one copy and missed in the other) with no shared helper enforcing it. Separately, the two `$()` capture sites inside `extract_substitutions` (the double-quote branch and the top-level branch) repeat the identical three-line `unescape = ... if segment_wants_reparse(...) else None; index = capture_and_mask(...); continue` block verbatim.
  * **Done when:** `extract_substitutions` derives its segment/leading-command context from `split_segments`'s own boundary logic (a shared helper, or `split_segments` output reused) rather than a second independent state machine, and the two duplicated `$()` capture blocks collapse to one shared call · S → `/assess-simplify claude-code/hooks/git_guard.py` reports it clear

### [TG-01.2] Token Efficiency
* **Target Release:** V1
* **Context (2026-09-09):** the always-on budget stays healthy (`scripts/validate.sh` tracks the figure). The bounded `git_guard.py` shell-parsing hardening pass this task group's last open task tracked is complete — the four Critical substitution-scanner bypasses it surfaced (comment-boundary reset, `command`-prefix reparse detection, and the two above) are closed or filed as their own `TG-01.1` tasks; the `command -v`/`-V` false-positive and the `extract_substitutions`/`split_segments` dedup remain open there. No task currently open in this task group.

### [TG-01.3] Skill Namespace & Assess Refactor
* **Target Release:** V1

### [TG-01.4] Workflow Loop & Hook Reliability
* **Target Release:** V1
* **Context (2026-09-09):** one band, one PR, by decision — the six tasks below chain on purpose (`(after:)` edges) because they all touch `workflow-loop/SKILL.md`, `scripts/test-hooks.sh`, or `docs/design-decisions.md`; the PR will exceed `git-pr-create`'s 12-file soft max and says so in its body. Target: a one-task band drops from 7 serial forks to 4, review findings below Critical/High stop triggering fix-and-re-review loops, comments get written by the implementer with live context and reviewed at band review, and every phase instruction is executable by the Skill tool (which has no `isolation` parameter). Full rationale: the "Workflow Loop Tune-Up" plan; verify each task with `make verify` — `workflow-loop/SKILL.md` sits near `validate.sh`'s 5000-token compaction cap, so every rewrite of it must net-shrink or hold.

#### [TSK-01.4.1] Restructure `workflow-loop` P2–P3: text drift out, worktree claims out, one band review with a severity floor [P: H] [TODO]
* **SUB-01.4.1.1** delete the duplicated paragraph at `claude-code/skills/workflow-loop/ways/sdlc.md:57-59` (two identical "Same round cap and round-numbered brief as P2.3" paragraphs — keep one)
  * **Done when:** `test "$(grep -c 'Same round cap and round-numbered brief as P2.3' claude-code/skills/workflow-loop/ways/sdlc.md)" -eq 1`
* **SUB-01.4.1.2** rewrite the P2.3 "Comment discipline" lens at `ways/sdlc.md:41` — there is no write-time comment guard anymore (`comments.py` is a lint); the lens becomes "comments are in the diff now (the implementer writes them, see TSK-01.4.3); a narrative, name-echo, or change-narrating comment is a Low finding the orchestrator deletes itself with one Edit, never a fork"
  * **Done when:** `! grep -q 'the guard blocks additions at write time' claude-code/skills/workflow-loop/ways/sdlc.md`
* **SUB-01.4.1.3** remove every `isolation: worktree` dispatch instruction and every "merge each worktree's diff back" sentence from `workflow-loop/SKILL.md` P2.1 (line ~107) and P2.4 (line ~143), from `ways/sdlc.md` P2.4 (line ~55), and from `workflow-decompose/SKILL.md` step 9 (keep the `## Parallel` section itself — reword its purpose to "recorded for a later parallel-implement capability; P2.1 dispatches serially today"); the one line that stays is the "Delegating outside the phases" table's "Parallel writers need `isolation: worktree`" note, which is about the Agent tool and is accurate. Reason, for the commit body: the Skill tool has no isolation parameter, no agent declares `isolation: worktree`, subagent worktrees branch from the default branch unless `worktree.baseRef: "head"` is set (it isn't), and `git_guard.py:840` denies merging any branch other than the protected base
  * **Done when:** `! grep -q 'isolation: worktree' claude-code/skills/workflow-loop/ways/sdlc.md claude-code/skills/workflow-decompose/SKILL.md` · `test "$(grep -c 'isolation: worktree' claude-code/skills/workflow-loop/SKILL.md)" -eq 1` · `! grep -qi 'worktree.*back' claude-code/skills/workflow-loop/SKILL.md`
* **SUB-01.4.1.4** rewrite P2.2, P2.3, P2.4 in `workflow-loop/SKILL.md` and `ways/sdlc.md` to this shape — **P2.2 Verify + commit**: run the task's `Done when` commands, paste output, then `/git-commit-message` against the task's diff alone and `git add` + `git commit`, one commit per task, then back to P2.0 for the next task; no review here. **P2.3 Band review**: runs once, after every task in the band is committed; dispatch `/assess-bugs`, `/assess-simplify`, and (only when a touched path is an auth/secret/boundary path or a rendered surface) `/assess-security` together in one parallel block as plain forks — no isolation, they are read-only after TSK-01.4.2; each brief carries the band summary, the `standards-*` skills the touched files load, and the round number; the orchestrator appends every returned finding row to `BACKLOG.md` under the matching domain in one Edit before triage (story-line shape per `backlog-modify`). **Severity floor**: Critical and High findings, plus Medium findings on correctness or a stated requirement, are fixed in this band via `/workflow-implement` and the fixed task re-enters P2.2; every other finding stays filed and open; the existing round cap (3, or 2 for a flagged parser/security file) still applies to the fixed set. **Exception**: a band of more than three tasks, or any task on a security boundary, also gets a per-task `/assess-bugs` at P2.2 before its commit. **P2.4 Closeout**: only `/changelog-write` for the whole band; no `assess-*` repeat, no `/backlog-audit` (TSK-01.4.4 moves the band-scoped audit into P3). Update the "Where each phase runs" table and every "Ends when" line to match; delete the P2.4 single-task skip paragraph and the "four standing closeout stories" sentence's reference to P2.4 running the assess suite
  * **Done when:** `grep -q 'Severity floor' claude-code/skills/workflow-loop/SKILL.md` · `! grep -q 'For a single-task band, skip' claude-code/skills/workflow-loop/SKILL.md` · `! grep -q '/backlog-audit' claude-code/skills/workflow-loop/SKILL.md` · `bash scripts/validate.sh`
* **SUB-01.4.1.5** rewrite P3 in `workflow-loop/SKILL.md`: drop the `/write-comments` call, the "every task's accumulated Comment Candidates" carry, and the `.claude/state/removed-comments.jsonl` feed and truncation (the removal ledger was retired with the guard rewrite; see `docs/design-decisions.md`); P3 becomes `/workflow-consolidate <band's TSK- IDs and task summaries>` → decide labels → `/git-commit-message` → one commit; also drop the P2.1 paragraph "A fork returns a result, never its reasoning — except `## Comment Candidates`"
  * **Done when:** `! grep -q 'Comment Candidates' claude-code/skills/workflow-loop/SKILL.md` · `! grep -rq 'removed-comments.jsonl' claude-code/skills/workflow-loop/` · `! grep -q 'write-comments' claude-code/skills/workflow-loop/SKILL.md` · `bash scripts/validate.sh`

#### [TSK-01.4.2] Read-only reviewers: `reviewer` loses `Edit`, the four reviewer-forked `assess-*` skills return findings only, `reviewer_guard.py`'s Edit/Write hook goes (after: "Restructure `workflow-loop` P2–P3: text drift out, worktree claims out, one band review with a severity floor") [P: H] [TODO]
* **SUB-01.4.2.1** `claude-code/agents/reviewer.md`: `tools: Read, Grep, Glob, Bash`; drop the "Never edit any file other than `BACKLOG.md`" boundary for "You never write. Any file, any tool, including a shell redirect."; output template keeps `## Findings` and drops `## Filed`
  * **Done when:** `! grep -q '^tools:.*Edit' claude-code/agents/reviewer.md` · `! grep -q '## Filed' claude-code/agents/reviewer.md`
* **SUB-01.4.2.2** in `assess-bugs`, `assess-security`, `assess-simplify`, `assess-code-conventions` (the four with `agent: reviewer`): delete the "Findings → backlog" section and every `--report` mention in body, `description`, and `argument-hint`; the report table is the whole output; add one sentence: "The caller files these rows to `BACKLOG.md`; you never write." Leave every other `assess-*` skill's `--report` alone — they don't fork to `reviewer`
  * **Done when:** `! grep -l -- '--report' claude-code/skills/assess-bugs/SKILL.md claude-code/skills/assess-security/SKILL.md claude-code/skills/assess-simplify/SKILL.md claude-code/skills/assess-code-conventions/SKILL.md` · `! grep -l 'Findings → backlog' claude-code/skills/assess-bugs/SKILL.md claude-code/skills/assess-security/SKILL.md claude-code/skills/assess-simplify/SKILL.md claude-code/skills/assess-code-conventions/SKILL.md`
* **SUB-01.4.2.3** retire the Edit/Write hook without breaking `git_guard.py`, which imports `REVIEWER_AGENT` and `is_allowed_path` from `reviewer_guard.py` (line 15) for its Bash-side check (lines 439–478): move `REVIEWER_AGENT` into `claude-code/hooks/lib/` (a new `lib/agents.py` or an existing lib module), change `git_guard.py`'s reviewer branch to deny every write target when `agent_type == REVIEWER_AGENT` (no `BACKLOG.md` exception anymore — the reviewer has no legitimate write), delete `claude-code/hooks/reviewer_guard.py`, remove its `PreToolUse` registration from `claude-code/settings.json`, replace its `scripts/test-hooks.sh` cases (R-series, including the R12 fail-open case) with two `git_guard.py` cases: reviewer `echo x >> BACKLOG.md` denies, and non-reviewer `echo x >> BACKLOG.md` is unaffected; use the atomic temp-file-then-`mv` write for `git_guard.py` per root `AGENTS.md`. `TSK-01.1.13`'s premise cites the R12 case — leave that task in place; the next audit re-verdicts it
  * **Done when:** `test ! -e claude-code/hooks/reviewer_guard.py` · `! grep -q reviewer_guard claude-code/settings.json claude-code/hooks/git_guard.py scripts/test-hooks.sh` · `bash scripts/test-hooks.sh`
* **SUB-01.4.2.4** docs: delete the `reviewer_guard.py` row from root `AGENTS.md`'s hook table and its fail-open sentence, update the `claude-code/agents/` row ("`reviewer` edits only `BACKLOG.md`" → "`reviewer` never writes"), and in `docs/design-decisions.md` add a short "Reviewers are read-only" section stating why (true parallel fan-out needs no isolation; the writer race on `BACKLOG.md` is gone; the orchestrator files the returned rows)
  * **Done when:** `! grep -q 'reviewer_guard' AGENTS.md` · `grep -q 'Reviewers are read-only' docs/design-decisions.md` · `bash scripts/validate.sh`

#### [TSK-01.4.3] Comments authored by `workflow-implementer` at task end, checked mechanically after every edit (after: "Read-only reviewers: `reviewer` loses `Edit`, the four reviewer-forked `assess-*` skills return findings only, `reviewer_guard.py`'s Edit/Write hook goes") [P: H] [TODO]
* **SUB-01.4.3.1** `claude-code/hooks/comments.py` gains a `hook` subcommand: reads a PostToolUse JSON payload from stdin, takes `tool_input.file_path`, runs the existing changed-lines `check` on that one file, and prints `{"hookSpecificOutput":{"hookEventName":"PostToolUse","additionalContext":"<one finding per line, same text as check>"}}` when there are findings, nothing when clean; exits 0 always (fails open — it is feedback, not a gate), including on a missing file, a non-source extension, or an unparseable payload. Add `scripts/test-hooks.sh` cases: a Go file with a `MISSING` site produces `additionalContext`; a clean file produces no output; a malformed payload exits 0 with no output
  * **Done when:** `printf '{"tool_name":"Edit","tool_input":{"file_path":"/nonexistent.go"}}' | python3 claude-code/hooks/comments.py hook; test $? -eq 0` · `bash scripts/test-hooks.sh`
* **SUB-01.4.3.2** `claude-code/agents/workflow-implementer.md`: add a `PostToolUse` hook (matcher `Edit|Write`, command `python3 ~/.claude/hooks/comments.py hook`) alongside the existing `Stop` hook; replace the `## Comment Candidates` output section and its two bullets with a final craft step "Comments, last": after every `Done when` passes, run `python3 ~/.claude/hooks/comments.py check --json`, then draft proposals only for sites that clear `write-comments/reference.md`'s bar (a non-obvious workaround, a rejected alternative, a discovered constraint, the mandatory ambiguous-discriminant `WHY`/`NOTE`) and pipe them through `python3 ~/.claude/hooks/comments.py apply`; never write a comment with Edit/Write; default is zero proposals; re-run `check` until it exits 0 or every remaining finding is listed as skipped with a reason; output gains a `## Comments` section (`spliced` lines, `apply`'s `dropped` reasons verbatim, skipped sites) — omitted entirely when nothing was spliced, dropped, or skipped. Keep the body under `validate.sh`'s caps
  * **Done when:** `grep -q 'comments.py hook' claude-code/agents/workflow-implementer.md` · `grep -q 'comments.py apply' claude-code/agents/workflow-implementer.md` · `! grep -q 'Comment Candidates' claude-code/agents/workflow-implementer.md` · `bash scripts/validate.sh`
* **SUB-01.4.3.3** `claude-code/skills/workflow-implement/SKILL.md`: add order step 7 "Comments, last — per your standing rules; the `Done when` commands must already be green" and point at `write-comments/reference.md` for the taxonomy
  * **Done when:** `grep -q 'reference.md' claude-code/skills/workflow-implement/SKILL.md`
* **SUB-01.4.3.4** `claude-code/skills/write-comments/SKILL.md`: reframe as the manual, on-demand path (a user-typed `/write-comments` over an arbitrary diff, or a repair pass when `comments.py check` is red in a repo's `verify`); remove "the workflow-implementer Comment Candidates it carried" from `argument-hint` and the body's "the implementer's `## Comment Candidates` entries" input; keep every judgment rule intact since the implementer now applies them
  * **Done when:** `! grep -q 'Comment Candidates' claude-code/skills/write-comments/SKILL.md` · `bash scripts/validate.sh`
* **SUB-01.4.3.5** docs: root `AGENTS.md` "Comments" section — replace "The `write-comments` skill is the sanctioned author path" with "`workflow-implementer` is the author path inside the loop, `write-comments` the manual one; both go through `comments.py apply`"; add the `comments.py hook` line to the hook table (registered on `workflow-implementer.md` frontmatter, PostToolUse, fails open); `docs/design-decisions.md` gets a "Comments are written by the fork that has the context" section naming the two reasons the P3 fork was retired (cold context, and comments landing after every reviewer had already run)
  * **Done when:** `grep -q 'comments.py hook' AGENTS.md` · `grep -q 'written by the fork that has the context' docs/design-decisions.md`

#### [TSK-01.4.4] `workflow-consolidate` absorbs the band-scoped backlog audit; `backlog-audit` becomes on-demand (after: "Comments authored by `workflow-implementer` at task end, checked mechanically after every edit") [P: M] [TODO]
* **SUB-01.4.4.1** `claude-code/skills/workflow-consolidate/SKILL.md`: `$ARGUMENTS` now carries the band's `TSK-` IDs plus summaries; add two ordered steps before the existing "Delete the completed stories" step — (a) for every `[BLOCKED]` task in `BACKLOG.md`, run its `Recheck:` condition; a pass clears the block (status back to `[TODO]`, `Blocked By:` bullet removed), a fail leaves it; (b) for each band `TSK-`, run every `SUB-` line's `Done when:` command and confirm zero exit before deleting it — a red command means the task stays, marked `[TODO]` with a one-line `Notes` entry in the report, never deleted. Load `backlog-modify` for the shapes
  * **Done when:** `grep -q 'Recheck' claude-code/skills/workflow-consolidate/SKILL.md` · `grep -q 'Done when' claude-code/skills/workflow-consolidate/SKILL.md`
* **SUB-01.4.4.2** `claude-code/skills/backlog-audit/SKILL.md`: delete the paragraph "`/workflow-loop`'s P2.4 runs this once per band…" and describe the skill as the full-file sweep a user types when the backlog feels stale, or that a session runs when P4's report notes it has not run in three or more bands; frontmatter unchanged (`validate.sh`'s reachability check and `repo-assess` still need it model-reachable)
  * **Done when:** `! grep -q 'P2.4' claude-code/skills/backlog-audit/SKILL.md` · `bash scripts/validate.sh`
* **SUB-01.4.4.3** `workflow-complete/SKILL.md` output gains one optional line: "Backlog last swept: <date of the last `/backlog-audit` per `git log -S'backlog-audit' --format=%ad -1`>" when that is more than three bands ago; `docs/design-decisions.md`'s "Every skill an autonomous workflow-loop run needs mid-loop carries neither key" section drops `backlog-audit` from the mid-loop list and says why
  * **Done when:** `grep -q 'Backlog last swept' claude-code/skills/workflow-complete/SKILL.md` · `bash scripts/validate.sh`

#### [TSK-01.4.5] Harden `verify_gate.py`: timeout blocks, records-only diffs skip, an identical failure stops the fork in 3 runs not 8 (after: "`workflow-consolidate` absorbs the band-scoped backlog audit; `backlog-audit` becomes on-demand") [P: H] [TODO]
* **SUB-01.4.5.1** catch `subprocess.TimeoutExpired` around `run(command, root)` and emit a block whose reason names the limit ("`make verify` exceeded 300 s — the task is not done; a suite this slow needs a narrower `.claude/verify.sh`"), counting toward the streak like any block; read the limit from `KOMODO_VERIFY_TIMEOUT` (integer seconds, default 300, invalid value falls back to 300). Today a timeout raises through to the `BaseException` handler and exits 0 — a silent green
  * **Done when:** `bash scripts/test-hooks.sh` with a new case: a fixture `.claude/verify.sh` that sleeps 3 s under `KOMODO_VERIFY_TIMEOUT=1` yields `"decision": "block"` with `exceeded 1 s` in the reason
* **SUB-01.4.5.2** records-only skip: after `tree_is_dirty`, parse `git status --porcelain` paths; when every dirty path is exactly one of `BACKLOG.md`, `docs/BACKLOG.md`, `CHANGELOG.md`, `README.md`, clear the streak and exit 0 without running the command; any other path (a `SKILL.md`, a `.py`, anything) runs the gate as before
  * **Done when:** `bash scripts/test-hooks.sh` with two new cases: dirty `CHANGELOG.md` + `BACKLOG.md` only → no block and the fixture verify script's marker file is not created; dirty `CHANGELOG.md` + `x.go` → the gate runs
* **SUB-01.4.5.3** identical-failure stop: store a sha256 of the combined failure output next to the streak count (same temp file, two lines); on a block whose hash equals the stored one, append "Same failure as the previous block." to the reason; on the third consecutive identical hash, exit 0 with `{"systemMessage": "verify_gate: identical failure three times — returning so the fork can report BLOCKED"}` and clear the streak, so a stuck fork returns `BLOCKED` in 3 verify runs instead of grinding to Claude Code's 8-block cutoff; a differing hash resets the identical counter but not the block streak
  * **Done when:** `bash scripts/test-hooks.sh` with a new case that invokes the hook three times against a fixture verify script that always fails identically: runs 1–2 return `"decision": "block"`, run 2's reason contains `Same failure`, run 3 returns exit 0 with a `systemMessage`
* **SUB-01.4.5.4** docs: `verify_gate.py`'s header comment, root `AGENTS.md`'s hook table row and "fails open" sentence (timeout is now a block; records-only diffs skip), `docs/design-decisions.md`'s verify_gate section (why 3 identical failures, not 8)
  * **Done when:** `grep -q 'KOMODO_VERIFY_TIMEOUT' AGENTS.md` · `grep -q 'identical' docs/design-decisions.md` · `make verify`

#### [TSK-01.4.7] P2.3 band review dropped perf-suite coverage — the "Performance" standing closeout story now clears nowhere [P: M] [TODO]
* **SUB-01.4.7.1** TSK-01.4.1's rewrite of P2.2–P2.4 (surfaced by that task's own implementer fork) replaced the old P2.4 (`assess-bugs`/`assess-security`/`assess-simplify` plus "confirm the perf suite ran") with a band review dispatching only `assess-bugs`, `assess-simplify`, and conditional `assess-security` — nothing in P2–P4 now checks the perf suite. Decide where perf-suite coverage lives in the new shape — folded into band review as a fourth conditional dispatch (a performance-sensitive touched path), or a standalone P2.4 step — and add it
  * **Done when:** `grep -riq 'perf' claude-code/skills/workflow-loop/SKILL.md` and the four standing closeout stories (Security review, Bug sweep, Code smell, Performance) each have a phase that clears them

#### [TSK-01.4.6] Fork tuning: `effort: high` on implementer and reviewer, `maxTurns` on every read-only agent (after: "Harden `verify_gate.py`: timeout blocks, records-only diffs skip, an identical failure stops the fork in 3 runs not 8") [P: M] [TODO]
* **SUB-01.4.6.1** `effort: high` in `claude-code/agents/workflow-implementer.md` and `claude-code/agents/reviewer.md` (agent frontmatter overrides `settings.json`'s `modelSettings`, so both run at medium today); `workflow-planner`, `engineering`, `scout` unchanged
  * **Done when:** `grep -q '^effort: high' claude-code/agents/workflow-implementer.md` · `grep -q '^effort: high' claude-code/agents/reviewer.md`
* **SUB-01.4.6.2** `maxTurns: 80` on `reviewer`, `maxTurns: 50` on `workflow-planner`, `maxTurns: 40` on `engineering`, `maxTurns: 20` on `scout`; not on `workflow-implementer` (its Stop gate is the bound); record the four numbers and the "read-only agents get a turn cap, the writer gets a gate" rule in `docs/design-decisions.md`
  * **Done when:** `grep -q '^maxTurns: 80' claude-code/agents/reviewer.md` · `grep -q '^maxTurns: 50' claude-code/agents/workflow-planner.md` · `grep -q '^maxTurns: 20' claude-code/agents/scout.md` · `bash scripts/validate.sh`
* **SUB-01.4.6.3** `make verify` green on the whole band, and `CHANGELOG.md`'s `[Unreleased]` carries one line per task above (written by `/changelog-write` at P2.4, not by hand here)
  * **Done when:** `make verify` · `grep -q 'reviewer' CHANGELOG.md`

---

## Archive
*Note: use this section strictly for abandoned, shelved, or deprecated initiatives to keep them separate from active work without losing historical ideas.*

_Nothing archived yet._
