# Project Backlog

## Convention Legend
* **Priority Tagging:** `[C]` Critical | `[H]` High | `[M]` Medium | `[L]` Low
* **Status Indicators:** `[TODO]` | `[IN_PROGRESS]` | `[BLOCKED]` | `[DONE]`
* **Hierarchy ID:** `EPIC-XX` -> `TG-XX.Y` (Task Group) -> `TSK-XX.Y.Z` (Task) -> `SUB-XX.Y.Z.N` (Subtask)

Format and rules live in the `backlog-modify` skill — load it before editing this file. `[DONE]` tasks stay until a sweep (`/backlog-audit`) moves them to `CHANGELOG.md` and removes them — this is not a log to hand-curate.

---

## [EPIC-01] Now, V1
*Goal: keep this toolkit's own hooks, docs, and skills correct and internally consistent.*

### [TG-01.1] Cross-Cutting
* **Target Release:** V1

#### [TSK-01.1.2] Machine identity for agent-run git/PR operations [P: M] [TODO]
* **SUB-01.1.2.1** agent-run commits and PRs currently authored as the user's own GitHub account (`gh auth`'s session token + local `git config user.name`) — create a machine-user account or GitHub App and wire its token into `gh`/`git` for agent-run operations
  * **Done when:** `gh auth status` inside an agent run shows the machine identity, not the user's own account, and a test commit/PR is authored under it

#### [TSK-01.1.3] Two Windows Git Bash CI failures need live diagnosis: `find_backlog`'s empty output, `gofmt` unresolved despite being on PATH [P: M] [BLOCKED]
* **Blocked By:** `external`
  * **Reason (2026-08-28):** `windows-hooks.yml`'s `test-hooks.sh (Git Bash)` job has two remaining failures across runs `33225168080` and `33228948540`. (1) I1/I2/I3/I6: completely empty stdout for the `inject_case` "full" fixture (a root with both `BACKLOG.md` and `CHANGELOG.md`), while "nested"/"junk"/"empty" behave correctly in the same run — no `CRASHED` marker, so `ci.main()` hit its own early `sys.exit(0)`, meaning `find_backlog(root)` returned `None` even though the file demonstrably exists. (2) F1: still "file was not reformatted" after adding `actions/setup-go@v5` — the run log confirms "Added go to the path" for `C:\hostedtoolcache\windows\go\1.26.7\x64`, yet `shutil.which("gofmt")` inside `auto_format.py` apparently still can't resolve it. Both need a live Windows shell to inspect actual `PATH`/`os.environ` contents inside the spawned Python process; guessing further fixes without verifying against the real failure risks masking the actual cause or silently breaking the passing macOS/Linux suite.
  * **Citation:** runs `33225168080` and `33228948540`, job `test-hooks.sh (Git Bash)` on `feat/windows-cross-platform-install`; `claude-code/hooks/context_injector.py:58` (`find_backlog`); `claude-code/hooks/auto_format.py:51` (`shutil.which`)
  * **Recheck:** re-run `test-hooks.sh (Git Bash)` on a Windows runner after adding temporary debug output — for I1/I2/I3/I6, print `root` and `os.path.isfile(...)` results inside `find_backlog`; for F1, print `os.environ.get("PATH")` and `shutil.which("gofmt")`'s result directly before the hook's own check — and inspect the actual values inside the native Windows Python process
  * **Note:** `scripts/test-hooks.sh` now skips these five cases when `uname -s` reports a Git Bash/MSYS/Cygwin environment (`IS_WINDOWS=1`), each printed as `SKIP` with a citation back to this task — the Windows CI job goes green without asserting these behaviors are actually correct there, and every other case still runs and gates real regressions on Windows normally. Remove the guard once diagnosed.
* **SUB-01.1.3.1** diagnose and fix why `find_backlog` can't see the "full" fixture's `BACKLOG.md` on the Windows Git Bash runner
  * **Done when:** `test-hooks.sh (Git Bash)`'s I1, I2, I3, I6 cases pass on a Windows CI run
* **SUB-01.1.3.2** diagnose and fix why `auto_format.py` can't resolve `gofmt` via `shutil.which` despite `actions/setup-go` adding it to `PATH`
  * **Done when:** `test-hooks.sh (Git Bash)`'s F1 case passes on a Windows CI run

#### [TSK-01.1.4] Bridge: `num_ctx` truncation on large summarizer payloads [P: M] [BLOCKED]
* **Blocked By:** `external`
  * **Reason (2026-08-28):** No file to edit in this repo — the bridge server (`generateRequest`, `agents.go`) lives in the separate `~/.komodo/bridge` deploy; `bridges/komodo-bridge/` here holds only prompt files and docs.
  * **Citation:** `bridges/komodo-bridge/` (prompt files and docs only, no Go source)
  * **Recheck:** bridge source is vendored into or made reachable from this repo — `find bridges/komodo-bridge -iname '*.go'` returns a match
* **SUB-01.1.4.1** fix `generateRequest`'s payload truncation against `num_ctx` in the bridge server once its source is reachable from this repo
  * **Done when:** a large summarizer payload no longer silently truncates against `num_ctx` in `~/.komodo/bridge`

#### [TSK-01.1.5] `reviewer` agent's Bash grant can write outside BACKLOG.md unexamined [P: M] [TODO]
* **SUB-01.1.5.2** [Medium] `reviewer`'s `Edit`/`Write`/`MultiEdit`/`NotebookEdit` path to non-`BACKLOG.md` files is now hook-enforced (`comment_guard.py`'s `check_reviewer_scope`, added 2026-09-01) — but its `Bash` grant is a separate, still-open bypass: `claude-code/settings.json`'s allow list permits `echo`, `printf`, and `tee` globally, and `git_guard.py`'s own redirect/`tee`/`cp`/`mv` protections gate only on `is_code_path()`, whose `EXTENSION_FAMILY` map (`comment_guard.py`) has no entry for `.json` or `.md`. `echo '{...}' > claude-code/settings.json` therefore reaches disk unexamined by either guard — `comment_guard.py` never fires (its matcher is `Edit|Write|MultiEdit|NotebookEdit`, not `Bash`), and `git_guard.py`'s redirect check treats `.json`/`.md` as a non-code path and skips it. This isn't new in this diff (the extension map is untouched here), but this band is what first hands an unattended, diff-reading fork the `Bash` tool needed to exercise it — settings.json is the file that stores which hooks even run, so this path can silently strip `comment_guard`/`git_guard`'s own `PreToolUse` registration for the rest of the session.
  * **Done when:** `is_code_path()` (or an equivalent check reachable from `git_guard.py`'s redirect/`tee`/`cp`/`mv` guards) covers `.json` and `.md`, so a shell redirect into `claude-code/settings.json` or `BACKLOG.md` is flagged the same way a redirect into a `.py`/`.go` file already is

### [TG-01.3] Token Efficiency
* **Target Release:** V1
* **Context (2026-09-01 audit):** the always-on budget is healthy (`scripts/validate.sh`: 1,149 of 2,000 tokens). The burn is inside a `/workflow-loop` band's orchestrator window, where the band's diff enters roughly six times (once per task in P2.3 via `assess-bugs` and `git-commit-message`, three times in P2.4 via `assess-bugs`/`assess-security`/`assess-simplify`, once in P4 via `git-pr-create`), and inside every fork, which loads the full `CLAUDE.md` hierarchy (this repo's root `AGENTS.md` alone is 7,157 tokens) before reading anything. Estimate for a 6-task band with a 3k-token task diff and a 15k-token band diff: ~190k orchestrator tokens today, ~50k after this group lands. Two harness facts every task below relies on, both verified against the Claude Code docs (`code.claude.com/docs/en/skills`, "Run skills in a subagent" and "Skill content lifecycle"; `code.claude.com/docs/en/sub-agents`, "What loads at startup"): (1) a `context: fork` skill "won't have access to your conversation history" — it gets the agent's system prompt, the SKILL.md body as its task, `$ARGUMENTS`, and the CLAUDE.md hierarchy; (2) a re-invoked skill whose rendered body is identical adds only a short "already loaded" note, but any skill that interpolates `$ARGUMENTS` renders differently per call and is appended in full every time.

#### [TSK-01.3.1] Run the review skills and the backlog audit in forks instead of the orchestrator window [P: H] [DONE]
* **SUB-01.3.1.1** `ways/sdlc.md:37` and `:51`, `workflow-loop/SKILL.md:123` and `:135`, and `assess-bugs/SKILL.md:11` / `assess-security/SKILL.md:11` all keep review in-window on the stated premise that "a fork saw the reasoning that produced the code and will agree with it". Per the docs a `context: fork` skill sees no conversation history, so the premise is false and the cost is real: `assess-bugs` (558 tokens) re-enters the window once per task because it interpolates `$ARGUMENTS`, each call reads the task diff into the window, step 1 of `assess-bugs`/`assess-simplify` loads `standards-*` skills into the window (`standards-go` 3,058 tokens; `standards-api-security` 3,800 + `review.md` 3,681) in direct contradiction of the guardrail at `workflow-loop/SKILL.md:179`, and P2.4 reads the whole band diff three separate times. Add a `reviewer` agent at `claude-code/agents/reviewer.md`: `tools: Read, Grep, Glob, Bash, Edit`, `model: sonnet`, `effort: medium`, body states it reads the diff cold with no access to the calling session, restates read-only git (`git_guard.py` enforces it anyway), forbids editing any file other than `BACKLOG.md`, and carries a mandatory output template of `## Findings` (the `Sev | Where | Claim | Scenario` table the three `assess-*` skills already define) plus `## Filed` (the `BACKLOG.md` story lines it appended) — `engineering`'s Answer/Evidence contract does not fit a findings table, which is why this is a new agent and not a reuse. Register it in root `AGENTS.md`'s layout table (line 12) and in `scripts/validate.sh`'s agents check if that check enumerates names.
  * **Done when:** `test -f claude-code/agents/reviewer.md && grep -q reviewer AGENTS.md && bash scripts/validate.sh`
* **SUB-01.3.1.2** add `context: fork`, `agent: reviewer`, `background: false` to the frontmatter of `assess-bugs`, `assess-security`, and `assess-simplify`; keep `$ARGUMENTS` (the fork receives it) and keep the `--report` path. Delete the "Never a fork of the session that wrote the code" sentence from each body — the agent definition now carries the cold-read guarantee. Their step "Load the `standards-*` skills named in `$ARGUMENTS`" stays as written: it now loads into the fork, which is the intent.
  * **Done when:** `test "$(grep -l '^context: fork' claude-code/skills/assess-bugs/SKILL.md claude-code/skills/assess-security/SKILL.md claude-code/skills/assess-simplify/SKILL.md | wc -l | tr -d ' ')" = 3 && test "$(grep -l '^agent: reviewer' claude-code/skills/assess-*/SKILL.md | wc -l | tr -d ' ')" = 3`
* **SUB-01.3.1.3** `backlog-audit` (1,179 tokens) reads `BACKLOG.md` in full and runs every `[DONE]` task's `Done when:` commands, all of it into the orchestrator at P2.4. Give it `context: fork`, `agent: workflow-implementer`, `background: false` — it writes `BACKLOG.md`, and `workflow-consolidate` already uses that agent for the same kind of record-keeping write. Its report section becomes the fork's return value.
  * **Done when:** `grep -q '^context: fork' claude-code/skills/backlog-audit/SKILL.md && grep -q '^agent: workflow-implementer' claude-code/skills/backlog-audit/SKILL.md`
* **SUB-01.3.1.4** rewrite the in-window prose to match: `workflow-loop/SKILL.md` "Where each phase runs" table rows for P2.3 and P2.4 name the `reviewer` fork; lines 123 and 135 drop "Never a fork of this session" and the reasoning after it; the guardrail at line 179 becomes "standards verification happens inside the review or implement fork, never in this window"; the delegation table at line 189 keeps "fresh subagent, never a `subagent_type: fork` Agent call" but spells out that a `context: fork` *skill* is fresh. `ways/sdlc.md:37` and `:51` drop the same sentence. Root `AGENTS.md`'s fifteen-skill paragraph and its "Every skill an autonomous run needs" paragraph gain the three `assess-*` skills and `backlog-audit` as forked phases with their agents.
  * **Done when:** `! grep -rn -i 'never a fork of' claude-code/skills/workflow-loop claude-code/skills/assess-bugs claude-code/skills/assess-security claude-code/skills/assess-simplify && bash scripts/validate.sh`

#### [TSK-01.3.2] Cut the root AGENTS.md to facts so forks stop paying 7,157 tokens of rationale each [P: H] [DONE]
* **SUB-01.3.2.1** every fork (`workflow-implement` once per task, `workflow-decompose`, `workflow-consolidate`, `write-comments`, and after TSK-01.3.1 every review call) loads the full CLAUDE.md hierarchy, which in this repo means `claude-code/AGENTS.md` (741 tokens) + `~/.claude/CLAUDE.local.md` (295) + root `AGENTS.md` (7,157). At ~9 forks per band that is ~74k tokens of preamble, most of it design history: why `verify_gate.py` sits on the agent, the naming-bucket rationale, the `readme`/`readme-audit` and `backlog-modify`/`backlog-audit`/`backlog-plan` split stories, the `no-op` directive anecdote. Keep in `AGENTS.md` only what a session needs to act: the layout table, the hook table plus the fail-open/fail-closed line, the comment-guard passing-shape table, the skill-contract key list and the `paths`/`skillOverrides` pairing rule, the No-static-references rule, the Context-budget bullets, and the Working-on-this-repo commands. Move every "why it was decided this way" paragraph verbatim into `docs/design-decisions.md` (new file, one `##` per decision, linked once from `AGENTS.md`), so nothing is lost and `git log -p` still finds it.
  * **Done when:** `python3 -c "import sys; sys.exit(0 if len(open('AGENTS.md').read())//4 <= 1800 else 1)" && test -f docs/design-decisions.md && bash scripts/validate.sh`
* **SUB-01.3.2.2** `claude-code/AGENTS.md:6` says "This file reaches the primary session only. A subagent spawned via the Task/Agent tool never inherits it". The sub-agents doc's "What loads at startup" lists the full CLAUDE.md hierarchy for every non-fork subagent, and the skills doc's fork table says a `context: fork` skill "also loads CLAUDE.md, except when the agent is Explore or Plan". Replace the paragraph with the true statement — every custom agent and every forked skill receives this file plus the project's CLAUDE.md chain, so a rule written here never needs restating in an agent body — and then delete the restated rules the agent bodies carry only because of the old claim (`engineering.md`, `scout.md`, `workflow-planner.md`, `workflow-implementer.md` each restate read-only git and never-edit, both hook-enforced).
  * **Done when:** `! grep -q 'reaches the primary session only' claude-code/AGENTS.md && bash scripts/validate.sh`
* **SUB-01.3.2.3** [Medium] SUB-01.3.2.2's own second half — delete the restated rules the agent bodies carry only because of the old inheritance claim — never landed. `claude-code/agents/engineering.md:23-24` ("Never edit." / "Never run git commands that change state...") and `claude-code/agents/scout.md:19-20` ("Never edit." / "Read-only git only...") and `claude-code/agents/workflow-implementer.md:22-23` ("Never edit the SDD." / "Read-only git...") still carry the redundant, hook-enforced restatements; none of the three files was touched when the inheritance claim was corrected. `workflow-planner.md` never carried the restatement, so it needs no change. This is exactly the per-fork fixed cost TSK-01.3.2 exists to cut.
  * **Done when:** `! grep -q -i 'never run git commands that change state' claude-code/agents/engineering.md && ! grep -q -i 'read-only git only' claude-code/agents/scout.md && ! grep -q -i 'read-only git' claude-code/agents/workflow-implementer.md`

#### [TSK-01.3.3] Collapse bundled-skill descriptions and account for them in validate.sh [P: M] [DONE]
* **SUB-01.3.3.1** the runtime lists Claude Code's bundled skills with full descriptions on every turn — `claude-api`, `dataviz`, `design`, `artifact-design`, `artifact-diagramming`, `artifact-capabilities`, `update-config`, `keybindings-help`, `schedule`, `loop`, `run`, `init`, `fewer-permission-prompts`, `workflow-authoring`, `security-review`, `simplify`, `code-review` — at roughly 2,000 tokens, more than this repo's whole always-on budget, and `scripts/validate.sh` cannot see them because their text lives in the binary. The skills doc's "Override skill visibility" section applies `skillOverrides` to bundled skills (its own example sets `"doctor": "off"`), and "Plugin skills are not affected by `skillOverrides`". Add each bundled name above to `claude-code/settings.json`'s `skillOverrides` as `"name-only"` (still typable from the `/` menu) — `"off"` for any the user confirms they never type. Leave `code-review` and `simplify` at `"name-only"` rather than `"off"`; they are typed. The plugin skill `skill-creator` cannot be collapsed; leave `enabledPlugins` as is.
  * **Done when:** `python3 -c "import json,sys; o=json.load(open('claude-code/settings.json'))['skillOverrides']; sys.exit(0 if all(o.get(k) in ('name-only','off') for k in ['claude-api','dataviz','design','artifact-design','artifact-diagramming','artifact-capabilities','update-config','keybindings-help','schedule','loop','run','init','fewer-permission-prompts','workflow-authoring']) else 1)"`
* **SUB-01.3.3.2** `"enableWorkflows": true` in `claude-code/settings.json` adds the `Workflow` tool definition and the `workflow-authoring` skill to every turn; `/workflow-loop` is this toolkit's machine and nothing in it calls the `Workflow` tool. Set it to `false`. If the user wants dynamic workflows back, this is the one line to flip.
  * **Done when:** `python3 -c "import json,sys; sys.exit(0 if json.load(open('claude-code/settings.json')).get('enableWorkflows') is False else 1)"`
* **SUB-01.3.3.3** root `AGENTS.md`'s Context budget section states that `validate.sh`'s total excludes bundled and plugin skills, that `skillOverrides` is the only lever for bundled ones and `/plugin` for plugin ones, and that `/context`'s Skills row is where the real listing size is read. `scripts/validate.sh` prints the same one-line caveat under its budget total so the number is never mistaken for the whole listing.
  * **Done when:** `grep -q -i 'bundled' AGENTS.md && grep -q -i 'bundled' scripts/validate.sh && bash scripts/validate.sh`

#### [TSK-01.3.4] Stop git-pr-create reading the whole band diff into the orchestrator at P4 [P: M] [TODO]
* **SUB-01.3.4.1** `git-pr-create/SKILL.md:15` reads "every commit ahead of the base (`git log <base>..HEAD`) and the full diff (`git diff <base>..HEAD`)". The skill body is 2,990 tokens and runs in-window from `workflow-complete`; on a 15k-token band that is ~18k tokens for a PR body whose content P3 already summarised per task in commit messages. Change the read to `git log <base>..HEAD` plus `git diff <base>..HEAD --stat`, and open a single file's diff only when its stat line and commit messages leave the change genuinely ambiguous. The `Size it` table already works off `--stat` numbers. Forking this skill was considered and rejected for now: it needs `Bash(gh pr create)`, and no existing agent contract fits a publish step — revisit only if the `--stat` read proves insufficient.
  * **Done when:** `grep -q -- 'git diff <base>..HEAD --stat' claude-code/skills/git-pr-create/SKILL.md && ! grep -q 'the full diff' claude-code/skills/git-pr-create/SKILL.md`

#### [TSK-01.3.5] Bring workflow-loop under the 5,000-token compaction re-attach cap and make the gates survive compaction [P: M] [TODO]
* **SUB-01.3.5.1** `workflow-loop/SKILL.md` is 5,048 tokens (`len(text)//4`, the same estimator `validate.sh` uses). The docs' "Skill content lifecycle" says compaction re-attaches "the first 5,000 tokens of each" invoked skill, so after the first compaction of a long band the orchestrator loses the tail: P4, Guardrails, Delegating outside the phases, The open hatch. Trim the file to at most 3,500 tokens by cutting justification prose while keeping every rule: the "this is what keeps…" / "this is also what removes P1's old cost problem" / "same reasoning as P2.3" sentences explain decisions the file does not need to re-argue at runtime — those explanations belong in `docs/design-decisions.md` from TSK-01.3.2 if they are worth keeping at all. Every phase's "Ends when" line, every skill name, and every table row stays.
  * **Done when:** `python3 -c "import sys; sys.exit(0 if len(open('claude-code/skills/workflow-loop/SKILL.md').read())//4 <= 3500 else 1)" && bash scripts/validate.sh`
* **SUB-01.3.5.2** `ways/sdlc.md` (895 tokens) is loaded with a `Read`, not a skill invocation, so compaction never re-attaches it and the P2.3/P2.4 gates it defines can silently vanish mid-band. Add one Guardrails bullet to `workflow-loop/SKILL.md`: after any context compaction, re-read the active `ways/` file before the next phase gate. Do not fold `sdlc.md` into `SKILL.md` — the "Adding a second way of working" contract depends on the ways file staying separate.
  * **Done when:** `grep -q -i 'compaction' claude-code/skills/workflow-loop/SKILL.md`
* **SUB-01.3.5.3** add a `scripts/validate.sh` check that fails when any `SKILL.md` exceeds 5,000 estimated tokens, printing the offender and its count, so the compaction cap is enforced the same way the listing budget is. `git-pr-create` (2,990), `standards-api-security` (3,800), and `standards-go` (3,058) pass today; the check is for the next file that grows past it.
  * **Done when:** `grep -q '5000' scripts/validate.sh && bash scripts/validate.sh`

#### [TSK-01.3.6] Fix the dead config-accessibility-output reference and shrink config-accessibility [P: L] [TODO]
* **SUB-01.3.6.1** `~/.claude/CLAUDE.local.md:7` says "Load `config-accessibility-output` before authoring anything longer than a screen"; no skill of that name exists — the skill is `config-accessibility`. The instruction has been dead since the rename, so the skill (2,445 tokens) never loads; the file's own bullet list under "Conversation — ADHD-calibrated" already carries the rules that matter. Note `~/.claude/CLAUDE.local.md.tmpl` is a dangling symlink to `claude-code/CLAUDE.local.md.tmpl`, which does not exist in the repo — so there is no tracked template to fix; correct the live `~/.claude/CLAUDE.local.md` line to name `config-accessibility`, or delete the line if SUB-01.3.6.2 makes the skill redundant.
  * **Done when:** `! grep -q 'config-accessibility-output' ~/.claude/CLAUDE.local.md`
* **SUB-01.3.6.2** `config-accessibility/SKILL.md` restates the same BLUF / no-preamble / short-paragraph rules `CLAUDE.local.md` already delivers every turn. Reduce it to what the local file does not say (the long-document structure rules, if any survive review), at most 800 tokens, or delete the skill directory and the `skillOverrides` entry if nothing survives.
  * **Done when:** `test ! -d claude-code/skills/config-accessibility || python3 -c "import sys; sys.exit(0 if len(open('claude-code/skills/config-accessibility/SKILL.md').read())//4 <= 800 else 1)"`

#### [TSK-01.3.7] git_guard.py denies a grep whose pattern text mentions a git verb inside backticks [P: L] [TODO]
* **SUB-01.3.7.1** observed 2026-09-01 in this repo: a `grep -n -E '<pattern>' <files>` whose single-quoted pattern contained backticks and the text `git log` was denied with reason "git log' changes repository state", and a second command that embedded `git log` inside `'"'"'` quoting was denied the same way; each false positive costs a full wasted turn plus a rewrite. A plain `grep -n "git log" README.md` passes (verified by piping the payload from a file into `python3 claude-code/hooks/git_guard.py`). Cause not verified — the likely spot is the command segmenter treating a backtick inside single quotes as a command-substitution boundary. Reproduce with a payload file (the repro command cannot be typed into a Bash call directly — the guard blocks it), find the segmenter fault, fix it, and add both shapes as allow cases to `scripts/test-hooks.sh` alongside a deny case confirming a real backtick substitution running `git log` in a write context is still caught.
  * **Done when:** `bash scripts/test-hooks.sh`

---

## Archive
*Note: use this section strictly for abandoned, shelved, or deprecated initiatives to keep them separate from active work without losing historical ideas.*

_Nothing archived yet._
