# Project Backlog

## Convention Legend
* **Priority Tagging:** `[C]` Critical | `[H]` High | `[M]` Medium | `[L]` Low
* **Status Indicators:** `[TODO]` | `[IN_PROGRESS]` | `[BLOCKED]` | `[DONE]`
* **Hierarchy ID:** `EPIC-XX` -> `TG-XX.Y` (Task Group) -> `TSK-XX.Y.Z` (Task) -> `SUB-XX.Y.Z.N` (Subtask)
* **Owner (optional):** an `Owner` row in a task's field table marks work only a person can resolve — a policy call, an external approval, a risk-acceptance decision. Absent means agent-executable by default.

Format and rules live in the `backlog-modify` skill — load it before editing this file. `[DONE]` tasks stay until a sweep (`/backlog-audit`) moves them to `CHANGELOG.md` and removes them — this is not a log to hand-curate.

---

## [EPIC-01] Now, V1
*Goal: keep this toolkit's own hooks, docs, and skills correct and internally consistent.*

### [TG-01.1] Cross-Cutting
* **Target Release:** V1

#### [TSK-01.1.1] Bridge: `num_ctx` truncation on large summarizer payloads [P: M] [BLOCKED]
| Field | Value |
|---|---|
| Blocked by | `external` |
| Reason (2026-08-28) | No file to edit in this repo — the bridge server (`generateRequest`, `agents.go`) lives in the separate `~/.komodo/bridge` deploy; `bridges/komodo-bridge/` here holds only prompt files and docs. |
| Citation | `bridges/komodo-bridge/` (prompt files and docs only, no Go source) |
| Recheck | bridge source is vendored into or made reachable from this repo — `find bridges/komodo-bridge -iname '*.go'` returns a match |

| Subtask | Work | Done when |
|---|---|---|
| `SUB-01.1.1.1` | fix `generateRequest`'s payload truncation against `num_ctx` in the bridge server once its source is reachable from this repo | a large summarizer payload no longer silently truncates against `num_ctx` in `~/.komodo/bridge` |

#### [TSK-01.1.2] `repo_root_of()`'s `@functools.lru_cache` builds its hash key before the function body runs, so an unhashable `cwd` (a `list`/`dict`) still raises an uncaught `TypeError` even after the earlier `TypeError`-catch fix [P: L] [TODO]
| Subtask | Work | Done when |
|---|---|---|
| `SUB-01.1.2.1` | surfaced by an `/assess-bugs` pass: `lru_cache`'s own key-hashing happens outside the `try`/`except` the fix widened, so it never sees an unhashable `cwd`. Currently unreachable — the sole call site (`claude-code/hooks/git_guard.py:498`) always passes a `str` — so this is latent, not live | a regression case (or inline check) confirms `repo_root_of(['a'])` no longer raises an uncaught `TypeError`, e.g. by validating/coercing `cwd` before the cached call, or wrapping the cache lookup itself |

### [TG-01.2] Token Efficiency
* **Target Release:** V1
* **Context (2026-09-09):** the always-on budget stays healthy (`scripts/validate.sh` tracks the figure). The bounded `git_guard.py` shell-parsing hardening pass this task group's last open task tracked is complete — all Critical substitution-scanner bypasses it surfaced (comment-boundary reset, `command`-prefix reparse detection, the `command -v`/`-V` false-positive, the `env` wrapper bypass, and the `extract_substitutions`/`split_segments` dedup) are closed as of `0.46.2` (see `CHANGELOG.md`). No task currently open in this task group.

### [TG-01.3] Skill Namespace & Assess Refactor
* **Target Release:** V1

#### [TSK-01.3.1] Root `AGENTS.md`'s "atomic write via temp-file-then-mv" rule for a live `claude-code/hooks/` file is unexecutable through Bash today [P: L] [DONE]
| Subtask | Work | Done when |
|---|---|---|
| `SUB-01.3.1.1` | `git_guard.py`'s comment-guard extension list blocks `cp`/`mv`/redirect onto `.py` (and `.json`/`.md`) targets for every agent, not just `reviewer` — so the temp-file-then-`mv` sequence `claude-code/AGENTS.md:20` prescribes for a nontrivial hook edit cannot actually run via Bash; the Edit/Write tool is the only path that works, which contradicts the rule's own wording. Surfaced live during TSK-01.4.2: deleting `reviewer_guard.py` before updating `settings.json` broke every subsequent Edit/Write in that session (the `PreToolUse` hook command itself was missing, which the harness treats as blocking); recovered with `ln` since `mv`/`cp`/redirect were themselves blocked. **Decided (2026-09-10): doc-wording fix only** — state Edit/Write as the sanctioned path, explicitly accepting its weaker (non-atomic, in-place) write guarantee; do not loosen `git_guard.py`, which is mid-hardening elsewhere (`TSK-01.4.9`) | `claude-code/AGENTS.md:20`'s rule names the Edit/Write tool as the sanctioned path for a hook-directory edit and no longer prescribes the shell `mv` sequence · `grep -q "Edit/Write tool" claude-code/AGENTS.md` |

#### [TSK-01.3.2] New skill: tech debt tracking (needs refinement) [P: L] [TODO]
| Subtask | Work | Done when |
|---|---|---|
| `SUB-01.3.2.1` | scope and write a `tech-debt` skill; what it should actually cover (capturing debt, triaging it, linking it to `BACKLOG.md`) isn't decided yet — needs refinement before it's buildable | — |

#### [TSK-01.3.3] New skill: git branching strategy (feature/branch vs smaller PRs) [P: L] [DONE]
| Subtask | Work | Done when |
|---|---|---|
| `SUB-01.3.3.1` | write a skill giving guidance on when to use a feature branch versus splitting work into smaller, direct PRs — name it `git-branching-strategy`, alongside this toolkit's other `git-*` skills | `claude-code/skills/git-branching-strategy/SKILL.md` exists and `bash scripts/validate.sh` passes |

#### [TSK-01.3.4] New skill: `/standards-zig` [P: L] [DONE]
| Subtask | Work | Done when |
|---|---|---|
| `SUB-01.3.4.1` | write a `standards-zig` skill covering Zig language/build/toolchain conventions, structured like the existing `standards-aws` and `standards-specs` skills | `claude-code/skills/standards-zig/SKILL.md` exists and `bash scripts/validate.sh` passes |

### [TG-01.4] Workflow Loop & Hook Reliability
* **Target Release:** V1
* **Context (2026-09-09):** one band, one PR, by decision — the six tasks below chain on purpose (`(after:)` edges) because they all touch `workflow-loop/SKILL.md`, `scripts/test-hooks.sh`, or `docs/design-decisions.md`; the PR will exceed `git-pr-create`'s 12-file soft max and says so in its body. Target: a one-task band drops from 7 serial forks to 4, review findings below Critical/High stop triggering fix-and-re-review loops, comments get written by the implementer with live context and reviewed at band review, and every phase instruction is executable by the Skill tool (which has no `isolation` parameter). Full rationale: the "Workflow Loop Tune-Up" plan; verify each task with `make verify` — `workflow-loop/SKILL.md` sits near `validate.sh`'s 5000-token compaction cap, so every rewrite of it must net-shrink or hold.

#### [TSK-01.4.9] `git_guard.py`'s `REVIEWER_AGENT` branch guards only its enumerated write syntaxes (redirect, `tee`, `cp`/`mv`, `sed -i`, `perl -i`, `python3 -c ... open(`) — it never gates `python3 <script> apply`, so `reviewer`'s remaining `Bash` tool can still invoke `comments.py apply` (which opens files for write internally) undetected, contradicting `reviewer.md`'s tightened claim that the agent "never writes... any tool" [P: H] [TODO]
| Subtask | Work | Done when |
|---|---|---|
| `SUB-01.4.9.1` | `scan_segment`'s `command in ("sed", "perl", "python", "python3")` branch returns unconditionally after checking only the `-c ... open(` pattern, so any other `python3 <path> <subcommand>` invocation (e.g. `python3 ~/.claude/hooks/comments.py apply` piped a proposal on stdin) passes through with no finding even when `agent_type == "reviewer"`; close the gap — e.g. make the `REVIEWER_AGENT` unconditional-deny apply to every `Bash` invocation of a script under `hooks/` known to write, not just the enumerated shell-syntax write vectors | a regression case in `scripts/test-hooks.sh` sends `agent_type: "reviewer"` with `command: "echo '{}' \| python3 ~/.claude/hooks/comments.py apply"` and asserts `git_guard.py` denies it |
| `SUB-01.4.9.2` | the round-2 fix (`comments_write_invocation`) only fires from inside the `command in ("sed", "perl", "python", "python3")` branch of `scan_segment`, which is gated on `command == os.path.basename(tokens[0])` matching one of those four literal interpreter names — `comments.py` itself is executable (`#!/usr/bin/env python3` shebang, mode 755), so invoking it directly (`claude-code/hooks/comments.py apply` or `./comments.py apply`) never reaches `comments_write_invocation` at all; reproduced: `{"tool_name":"Bash","tool_input":{"command":"claude-code/hooks/comments.py apply"},"agent_type":"reviewer"}` piped into `git_guard.py` exits 0 with no deny, while the otherwise-identical `python3 claude-code/hooks/comments.py apply` is correctly blocked | a regression case in `scripts/test-hooks.sh` sends `agent_type: "reviewer"` with `command: "claude-code/hooks/comments.py apply"` (no `python3`/`python` prefix) and asserts `git_guard.py` denies it |
| `SUB-01.4.9.3` | the round-3 fix (`comments_write_invocation` scanning `tokens[0]` too, matched against `COMMENTS_SCRIPT_BASENAME`) still gates entry on `scan_segment`'s outer `command in ("sed", "perl", "python", "python3", COMMENTS_SCRIPT_BASENAME)` check and on an exact-string basename match — three variants reproduced live against `git_guard.py` with `agent_type: "reviewer"`: (1) `cat ~/.claude/hooks/comments.py \| python3 - apply` — `python3 -` reads the script body from stdin, so `tokens` for that segment is `['python3', '-', 'apply']` with no token basename-matching `comments.py`, `<ALLOW>`; (2) a same-directory copy or symlink invoked under a different name, e.g. `./cw apply` where `cw` is comments.py's content under another filename — outer command is `cw`, never enters the `sed`/`perl`/`python`/`comments.py` branch at all, `<ALLOW>`; (3) `python3 /path/to/Comments.PY apply` — on this host's default case-insensitive filesystem that resolves to the same file as `comments.py`, but `os.path.basename(token) == COMMENTS_SCRIPT_BASENAME` is a case-sensitive compare, `<ALLOW>` | three regression cases in `scripts/test-hooks.sh`, each with `agent_type: "reviewer"`, assert `git_guard.py` denies: `cat ~/.claude/hooks/comments.py \| python3 - apply`, a same-content script invoked under a non-`comments.py` basename, and a same-file differently-cased path |
| `SUB-01.4.9.4` | the round-4 fix's content-comparison fallback (`comments_script_signature`/`is_comments_script`) requires an exact byte-for-byte match against `comments.py`'s real content, so it correctly identifies a hardlink (`os.path.samefile` catches it) but not a functionally-identical copy that differs by even one byte — reproduced live: a copy of `claude-code/hooks/comments.py` with a single byte flipped mid-file (any no-op edit — whitespace, a renamed unused variable, a comment character — produces the same effect), saved under an unrelated basename, run as `python3 <copy>.py apply` end-to-end through `scan_command` with `agent_type: "reviewer"`, returns zero findings; `is_comments_script` returns `False` for it despite the file still opening targets for write exactly like the real script. No false positive was found in the same pass — an unrelated file piped through `python3 -` (`cat README.md \| python3 - --version`) correctly produces no finding, and the real `comments.py` piped through `python3 -` correctly denies | a regression case in `scripts/test-hooks.sh` sends `agent_type: "reviewer"` with a command invoking a one-byte-modified copy of `comments.py` (by content, any basename) with the `apply` subcommand, and asserts `git_guard.py` denies it — e.g. by relaxing the content check to a normalized/fuzzy comparison (strip comments/whitespace, or hash a canonicalized AST) rather than exact bytes, or by moving the write-detection boundary to something a one-byte edit can't dodge (e.g. flagging any script invocation whose positional args include a `COMMENTS_WRITE_SUBCOMMANDS` token alongside a script path outside the repo's own `hooks/` tree, for the reviewer agent specifically) |
| `SUB-01.4.9.5` | **(2026-09-10) — a harness-attested-token approach was tried and found broken, don't re-attempt this exact design.** Built and self-tested (not just proposed): `git_guard.py` stamps a fresh random `KOMODO_APPLY_TOKEN` into every non-reviewer Bash call via `updatedInput` (`export KOMODO_APPLY_TOKEN=<token>; <cmd>`), writes the same value to a state file, and rewrites a reviewer call to `unset KOMODO_APPLY_TOKEN; readonly KOMODO_APPLY_TOKEN; <cmd>`; `comments.py apply` refuses unless the env var matches the state file's current value within a 30s window. Broken by two independent facts, both reproduced live against the actual (now-reverted) code: (1) `readonly` blocks a later shell `export`/assignment-prefix but **not** `env VAR=val cmd`, which builds the child process's environment directly — `env KOMODO_APPLY_TOKEN=<value> python3 comments.py apply` bypasses it entirely; (2) the state file the token lives in is an ordinary, world-readable file at a predictable temp-dir path — `reviewer`'s `Read` tool (never routed through `git_guard.py`'s Bash-only `PreToolUse` hook) reads the current valid token directly, no Bash call needed. Combined: `env KOMODO_APPLY_TOKEN=$(Read the state file) python3 comments.py apply` passes the check with the real token, reproduced end-to-end (`unset ...; readonly ...; env KOMODO_APPLY_TOKEN=<real> python3 comments.py apply` → `comments.py apply` proceeds, exit 0). Root cause, not specific to this implementation: this sandboxed, single-OS-user environment has no channel to carry a secret from one hook invocation forward to a later, independent process's runtime check that isn't *also* reachable through some other unscoped tool (`Read`) or environment-construction primitive (`env`) `reviewer` already has. Reverted via `git stash` (not deleted — recoverable if a future attempt wants to study exactly what broke) rather than committed. | N/A — this line records a dead end, not an open task. If SUB-01.4.9.4 is ever attempted again, read this first; a design needs to survive both bypasses above, or explain why this environment's tool set makes them inapplicable, before it's worth building |
| `SUB-01.4.9.6` | **(2026-09-10) — decided direction for closing SUB-01.4.9.4: stop recognizing bad commands, allowlist good ones.** Both `.4` and `.5` failed the same way — each tries to *recognize* a dangerous `comments.py apply` invocation (by content hash, or by a runtime-checked token), and recognition is a blocklist problem with infinite variants in an environment with no per-agent sandboxing, where `env VAR=val cmd` bypasses shell `readonly` and `Read` bypasses the Bash-only `PreToolUse` hook. The fix: for `agent_type == REVIEWER_AGENT`, stop trying to identify `comments.py` specifically — deny every interpreter invocation (`python3`, `python`, `perl`, `sed -i`, etc.) outright by default, and allow only a short enumerated set of genuinely read-only binaries, the same pattern `git_guard.py` already uses for git's own read-only subcommand allowlist (`log`, `diff`, `show`, `status`, ...). A byte-different copy of `comments.py` is irrelevant under this design since no script execution reaches `reviewer` at all; there is no runtime secret left for `env` to unshadow either. Required first step, not optional: audit what `reviewer` forks actually invoke via Bash today (`scripts/test-hooks.sh`'s existing `agent_type: "reviewer"` cases, plus `assess-bugs`/`assess-security`/`assess-simplify`'s skill bodies for any Bash command they tell the fork to run) so the allowlist is seeded with every legitimate command in current use before the deny-by-default ships — otherwise the first real review run silently breaks on a command that never made the list. Does not mark `SUB-01.4.9.4` done; that stays open until this ships | the audit above is written down as a list of every distinct Bash command shape `reviewer` currently issues; `git_guard.py`'s `REVIEWER_AGENT` branch is rewritten to deny every interpreter invocation by default with that audited list as the allowlist; `scripts/test-hooks.sh` gains regression cases proving both directions — every audited legitimate command still passes, and a previously-undetectable bypass (a byte-modified copy of `comments.py`, or any other script never on the list) is denied regardless of content |

---

## Archive
*Note: use this section strictly for abandoned, shelved, or deprecated initiatives to keep them separate from active work without losing historical ideas.*

_Nothing archived yet._
